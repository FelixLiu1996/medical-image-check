# 测试与验收初稿

状态：框架已确定，验收数据和准确率目标待定。

## 开发验证命令

```bash
python -m ruff check .
python -m ruff format --check .
python -m pip check
QT_QPA_PLATFORM=offscreen python -m pytest
```

当前自动化测试使用临时目录生成完全人工数据，覆盖图片文件指纹、跨格式解码像素、多页 TIFF、旋转与 JPEG 压缩整体近似、旋转/缩放/压缩裁剪、两张裁剪图部分重叠、复合图规则分隔/单图回退/部分勾选/临时裁剪清理/原图坐标回映、无关图像负例、Western blot 曝光/翻转/同图 Copy-Move/单条带、Dot blot 弱斑点、3/8 局部子集、旋转/缩放/对比度、暗底划痕和相同排列不同内容负例、AUTO 斑点准入/科学示意图拒绝/Western 覆盖率与混合页路由、显式 Dot blot 路由旁路、Dot 外观计算缓存和覆盖优先候选预算、Dot blot 本地标签清单指标、荧光单通道与 Merge/跨通道同视野/同通道复用、病理不同倍率/同倍率局部复用，以及专项证据 GUI/Excel/HTML/PDF 报告；同时覆盖图片/数据工作区导航、图片内容类型路由、文件类型过滤、单模式检测器隔离、扫描暂停/继续/取消、阶段计时与暂停扣除、NVIDIA/OpenCV CUDA 探测隔离、性能画像项目迁移和无路径/证据 JSON 诊断、损坏图片隔离、xlsx/xlsm/xls/csv 数值读取与原表上下文高亮、隐藏工作表、公式无缓存、表头元数据、公式/`rescaled` 派生列正常关系、重点/全部线索切换、有界重点队列、原文件不变、项目版本迁移及设置保存、轻量反馈更新/清除/重扫继承/状态筛选/项目恢复、Excel/JSON 反馈清单、数字片段风险分级、自定义/默认近似容差、单次与连续四则运算、零乘积/常量降噪、跨工作表公平候选选择、连续片段、乱序、少量修改、稳健线性、二维区域、汇总/分布统计低风险候选和 Qt 窗口/打包模式冒烟。仓库测试数据不包含真实实验数据。

GitHub CI 在 Windows 执行全部测试；Linux 用于验证无界面核心逻辑，不执行 Windows 产品界面的 Qt 冒烟测试。

Windows portable 工作流在源代码测试后构建 standalone 目录，并执行打包程序的 `--smoke-test`；该模式除创建/关闭窗口外，还在临时目录实际生成 Excel、HTML、PDF 三种报告并检查文件签名，用于发现 ReportLab/Pillow 或模板漏打包。它仍不能代替干净 Windows 10/11 人工扫描、字体显示、打印与导出验收。

PDF 本地排版验收使用软件生成包含中文总览、结果表、荧光图像证据和扫描提示的 A4 样例，通过 Poppler 渲染全部页面并目视检查；同时用 pypdf 验证标题、结果、证据和提示的中文文本层可检索。Windows 仍需验证系统微软雅黑/宋体发现与不同阅读器/打印机兼容性。

## 测试层次

1. 单元测试：规范化、哈希、数值容差、规则评分、版本迁移。
2. 集成测试：图片/Excel 导入到结果、历史库和报告完整流程。
3. 黄金数据测试：固定输入、参数和版本得到稳定结果。
4. 算法验收：正例召回、负例误报、定位误差和风险分级。
5. 性能测试：CPU/GPU 时间、内存、磁盘、索引和恢复。
6. 桌面端测试：安装、免安装、升级、异常退出和 Windows 兼容。
7. 报告测试：Excel/HTML/PDF 内容一致和视觉可读。

## 必测场景

### 图像

- 完全相同、格式变化和元数据变化。
- 压缩、缩放、旋转、翻转、调色、裁剪和标注。
- 局部重叠和单图 Copy-Move。
- 局部候选索引在数百/数千图规模下的召回、运行时间和峰值内存。
- 复合图规则分隔、普通单图整页回退、过拆/欠拆预览、部分取消、TIFF 页码和原图坐标回映。
- Western blot 泳道、条带、背景及局部编辑。
- Dot blot 斑点缺失/伪影、弱斑点、近连续局部子集、裁剪、缩放、旋转、镜像、灰度和对比度变化，以及相同排列但局部内容不同的负例。
- 荧光同视野通道、Merge、跨组复用和正常负例。
- 病理不同倍率、连续切片、局部复用和形态相似负例。
- CPU/GPU 结果一致性和 GPU 回退。

### Excel

- xlsx/xls/xlsm/csv 和隐藏工作表。
- 公式缓存存在、缺失及宏不执行。
- 完整数值、连续区域、顺序打乱和少量修改。
- 不同单元格的 4 位以上数字片段。
- 0.01%、0.1%、1% 容差边界和接近 0 的值。
- 固定倍数、偏移、加减乘除目标和偶然巧合负例。
- 全零、常量、身份运算及前序工作表占满结果上限的降噪/公平性场景。
- 原始列与同排公式/`rescaled` 派生列的正常关系，以及相似表头但无派生关系的负例。
- 跨规则重复解释只在重点队列保留一项、全部线索不丢失，以及全局/工作簿/工作表配额边界。
- 文本、日期、空白、加密文件和损坏文件。

## Alpha 真实样例回归（不入库）

- 42 个公众号案例初步形成 34 对通用 validation；来源彩框几何复核随后排除病理组织 #02（Azan 染色被误识别为洋红框）。当前有效集为 33 对：25 对阳性、8 对阴性；按 Western blot、病理、荧光和通用图像路由后的同集回归为 25 TP/0 FP/0 FN/8 TN。由于该批样本参与过错误分析和阈值开发，结果只用于开发回归，不是独立 test 或可对外宣传的准确率。
- `panel-split-1` 合入且保持默认关闭后，当前 33 对仍应全部回归；这不等于自动拆图准确率。对 `official-papers` 中 299 张正式全文图片的无标签冒烟生成 2,836 个候选：11 张整页回退、288 张多候选、单图最多 46 个，约 8.56 秒。该分布可能包含欠拆和过拆，必须用人工面板边界另建拆分评测集。
- 自动模式的常规页面准入使用最长边 512 像素的暗分位 P12 与局部响应 P25/P55；常规组件至少满足 0.70 短长轴比、0.65 圆度和 0.70 实心度，额外 P10 响应层只进入大图内严格小斑点路径。科学示意图、共享基线且厚度近似一致的柱状图柱条，以及单块 Western 覆盖至少 35% 或多块总覆盖至少 35% 且联合覆盖至少 25% 的主导页不得进入 Dot 完整提取；大图内小 Dot 阵列和只有两个零散小 Western 区域的混合页面仍应准入。准入页面的结果只显示“局部结构”，不得据此宣称实验类型；显式 Dot blot 模式应绕过自动准入并显示 Dot blot 专项证据。
- `generic-image-local-5` 合成回归覆盖白底科学图表布局抑制、彩色组织保留、局部几何后的实际区域内容验证、低纹理小条带位移/翻转、细长条带行对称边框差异、显微区域镜像、被主索引漏掉的小型组织裁剪补充召回和无关区域拒绝；Western、Dot blot、荧光和病理测试另覆盖 AUTO 保守路由与显式模式边界。对超过 128 个解码页特征的大批次，局部/小区域主候选不超过 512/256 对且每个页特征不超过 16 对，补充小图候选不超过 64 对且每个页特征不超过 8 对。回归还要求 local、small-primary 和 fallback 满桶后尾部 `(64,65)` 仍可互相召回，70 个同桶特征均至少被一个候选覆盖，且 dense clique 外的独立低票 pair 不被 64 对全局预算饿死。预算在已召回集合与固定容量内先增加未覆盖端点，再按原排名补足，减少单一密集组垄断，但不保证排列不变或完整召回。`panel-sift-fallback-1` 覆盖不超过 64 个 Panel 时的 384 描述子有界 SIFT 排序；`panel-sift-fallback-2` 另覆盖大批次按原始 Figure 对最多 6 个候选、全局 384 个排序输入的边界，并拒绝同 Figure 补充。大批次两层补充并集最多 128 对、每个 Panel 最多 16 对，因此局部/小区域 verifier 理论上限分别为 640/384 对、每个 Panel 32 对。另以 48 张独立生成的无关灰度小条带形成 1,128 个可能图片对，输出 0 条小区域复用结果，端到端约 4.5 秒。该压力测试验证候选有界和常见无关小图降噪，不代表真实数据 specificity。
- `generic-image-local-6` 另覆盖多泳道横向连续子集、单条带拒绝、“只有进入专用短名单才允许运行子集 verifier”，以及“泳道排版相似但局部细节不同”的合成拒绝。Panel 子集短名单要求跨原始 Figure，每个 Figure 对最多 4 对、全局最多 64 对、每个 Panel 最多 8 对；固定 50%–80% 连续窗口、三路粗相关和 192×48 行列残差细节门禁共同约束输出。
- 2026-09-03 医学人员返回的 32 组阴性校准候选全部判为不重复。`generic-image-local-4+western-blot-3+dot-blot-5+fluorescence-2+pathology-3` 调整后，32 个原候选 ID 均不再出现；涉及的 28 个案例总候选由 1,856 条降为 183 条。124 例 Panel 全量回归由 7,409 条降为 1,148 条，其中 61 个可评阴性案例由 4,588 条降为 498 条、含候选案例由 59 个降为 53 个；整图由 110 条降为 39 条。整图与 Panel 共 248 次均完成、0 错误、0 次超过 120 秒；Panel 单例 P50/P95/max 为 13.096895/37.237628/66.652040 秒，整图为 3.696857/20.481100/35.929333 秒。29 组确认阳性关系仍为宽松区域 2/29，严格区域 1/29；旧 34 对开发回归仍为 25 TP/0 FP/0 FN/9 TN。该批已经用于阈值选择，以上结果只证明已知问题和回归边界被覆盖，不能报告为独立 Precision/F1 或泛化准确率。
- 第一版医学图片逐对 validation 共 39 对：26 对阳性、13 对阴性，覆盖 Western blot、免疫荧光、普通病理和细胞明场图；Dot blot 按用户决定暂缓。去除可见彩框并优先换用未画框原图后，`western-blot-1` 基线为 TP=11、FP=1、FN=15、TN=12；`western-blot-2` 定向修正后为 TP=16、FP=0、FN=10、TN=13（precision 1.000、recall 0.615、specificity 1.000）。这是同一小样本上的开发回归，不是正式准确率；全部样例仍在 validation。
- `western-blot-2` 在 11 对 Western validation 上为 7 TP/0 FP/0 FN/4 TN；医学人员确认的 5 个原 FN 均已检出、1 个原 FP 已排除，Casp11/NEDD4 目标条带及其 beta-actin 困难阴性仍被正确排除。免疫荧光仍为 5 TP/0 FP/4 FN/4 TN；病理为 1 TP/0 FP/1 FN/5 TN；明场/通用为 3 TP/0 FP/5 FN，当前尚无该类独立阴性。条带数量差异不作为直接排除规则。
- 通用逐对评测器支持整图、同图双区域、不同文件双区域、逐类型检测、Western 单条带开关、来源组 split 隔离和分类型指标，格式见 `docs/IMAGE_PAIR_EVALUATION.md`。真实图片、裁剪图、本地清单和完整结果均位于 Git 忽略目录，不进入仓库。
- 用户提供的 4 张同源 Dot blot 图片仅在本地只读回归：`dot-blot-2` 自动模式输出 6 组两两 Dot blot 关系且没有其他专项串类，无读取问题；耗时约 1.8 秒。样本量过小，不用于计算准确率。
- Dot blot 等间距负例合成微基准：25 张独立四斑点图片提取 28 个阵列候选约 1.6 秒、验证约 2.3 秒，输出 0 条专项结果；该微基准只验证候选上限与常见排列降噪，不代表真实数据性能或准确率。
- Dot blot 真实/公开授权样本采用 `docs/DOT_BLOT_EVALUATION.md` 的本地清单逐对标注并按来源组隔离；仓库只保存评测器和合成测试，不保存真实图片或本机清单。
- 用户提供的 13 个 Excel 工作簿仅在本地只读回归：`excel-advanced-4` 默认参数输出 1,775 条全部线索、22 条正常派生关系和 50 条重点候选；MOESM9 连续片段、MOESM14 配对和为 2、MOESM15 结构重复均保留在重点队列，读取与检测约 6.36 秒。修复前为 9,864 条，上一算法版本约 1,800 条。
- 真实样例路径和内容不得写入测试夹具、提交记录、报告示例或公有 Release；后续仍需要授权且可公开或内部保管的独立正负例验收集。

### 项目和可靠性

- 暂停、继续、取消、崩溃恢复。
- 准确/误报/正常关联/清除标记、复核状态筛选及未标记默认行为。
- 标记项目保存恢复、同一算法版本的相同稳定结果重扫继承、算法版本变化时不继承，以及输入/参数变化后旧结果失效。
- Excel/JSON 反馈清单只包含已标记结果，不复制或修改原始文件。
- 源文件移动、变化和丢失。
- 轻量/完整项目包跨电脑导入。
- 历史库导出、导入、合并和重新索引。
- 扫描前后源文件指纹不变。

## 验收数据

用户已提供第一批图片样例，但仍需逐对完成医学确认、补足独立阴性并按来源建立隔离 test 集。真实或授权不明的数据不得提交到公有仓库。

最终准确率、召回率、误报率和定位容差在数据集确认后补充，不得在缺少数据时虚构指标。

## 性能基准

至少记录：机器配置、软件/模型/算法版本、参数、冷启动/热启动、索引时间、精查时间、峰值内存、磁盘增长和 CPU/GPU 利用率。

`0.1.0a12` 起，每次完成扫描都会生成 performance schema 1：记录总时长、扣除协作暂停后的有效运行时长，以及输入收集、图片解码、通用/专项特征与验证、Excel 读取/规则和结果整理等实际阶段。`0.1.0a18` 增加 `image.dot_blot_routing`、`image.generic_candidate_selection` 和 `image.local_geometric_candidates` / `image.small_content_candidates` / `image.dot_blot_candidates`；新增 `*.candidates` 的 `items` 表示实际进入精查的候选对数，既有三个 `*.verification` 的 `items` 继续表示最终结果数，保持 schema 1 的历史语义。其余阶段的 `items` 仍按各自输入或输出定义；所有计数只能辅助解释同一阶段，不能直接跨阶段比较工作量。GUI 可导出 JSON；文件不包含原始数据、输入路径、查重位置或结构化证据。

2026-09-01 覆盖优先预算、满桶滚动候选与混合页路由修正后的 124 例双模式重跑使用本地评测 runner 的每例 120 秒 watchdog（不是产品内建超时）且单进程执行：整图和 Panel 共 248 次扫描，0 超时、0 错误。累计输入 793 次正式图引用（跨案例去重为 776 张唯一图）；整图单例 P50/P95/max 为 2.73/14.56/27.25 秒，Panel 为 7.06/27.10/48.00 秒。该结果证明本轮评测的超时已消除，但这 124 例已用于问题诊断，属于 post-unseal validation/regression，不是独立 test 或 CI 性能门。整图案例级筛查对 19/35 个可评阳性给出候选；Panel 对 35/35 个可评阳性均给出候选，对 61 个可评阴性中的 59 个也给出候选。Panel 阴性的 4,588 条正向候选中通用局部几何占 3,941 条、Dot blot 占 9 条。该阶段尚无 Figure/Panel 区域真值，指标保持 `N/A`；后续 29 组阳性关系已补算关系级召回，但正式 precision/F1 仍须等待阴性复核。

2026-09-02 增加公众号关系标准答案草稿与复核包测试，覆盖正文图片上下文、中文/英文 Figure 简写、反驳语义不自动晋升、同色框双区域和完整离线包生成。真实 44 个阳性案例的包仅在 Git 忽略目录验证：42 个页面可用、103 张来源图、78 张复核卡、94 组源图 A/B、32 组正式映射候选；浏览器最终 291/291 个图片元素加载，反馈选择刷新保持和筛选正常。该包明确不包含算法发现，所有候选在医生反馈返回和校验前均不可计分。

2026-09-03 将医生复核面收敛为 32 组已有正式映射的单关系卡；自动化测试新增正式端点资产解析、正式局部裁剪、带红框完整 Figure、简化反馈 schema 2 和旧三层控件移除断言。真实包覆盖 18 个案例，0 个资产失败；ZIP 218 个条目和 manifest 217 个文件的尺寸/哈希全部通过。浏览器实测 32 卡、96 个三选一反馈控件、32 个备注框及 224 张图片，图片 0 失败；选择后进度为 1/32，刷新后保持，完成筛选仅显示 1 项，控制台无错误。

2026-09-03 对返回的 32 组映射反馈完成 ID、批次、范围和决定值校验：29 组 `correct` 覆盖 17 个案例，1 组 `wrong`、1 组 `pending`、1 组 `uncertain` 均排除计分。29 组确认关系使用原 124 例固定扫描结果进行关系级回归：整图为 0/29；Panel 自动拆分为 2/29，关系级召回 0.068966，95% Wilson 区间 0.019121–0.219646；严格区域口径为 0/29。两个宽松命中分别由 `image.small_region.content_reuse` 和 `image.local.geometric` 产生。路径诊断显示 Panel 有 13/29 组在同一算法 Finding 中同时出现两个正确正式图路径、另有 12/29 组的两个路径只分散在不同 Finding 中，说明主要问题是候选端点配对与区域定位，而不是“阳性文章完全没有候选”。此前 35/35 只能作为案例级筛查结果，不能当关系级召回。

2026-09-04 `panel-split-2` 的同集回归按 29 组确认关系的正式 A/B 路径和区域重新计分：宽松区域命中 6/29，关系级召回 0.206897（95% Wilson 0.098461–0.383901）；严格区域命中 4/29，严格召回 0.137931（95% Wilson 0.054974–0.305590）。全部 80 个阴性案例中，只有 4 个案例的自动 Panel 集合发生变化；对应扫描候选 498 -> 497，没有候选净增加，32 个已复核阴性原候选仍未出现。旧 34 对开发回归仍为 25 TP/0 FP/0 FN/9 TN。新增合成测试覆盖规则条带、显微图行、彩色热图、不规则柱状图和单页展开上限；macOS ARM64/Python 3.12.14 下 Ruff、`pip check`、`git diff --check` 和 Qt offscreen pytest 188/188 均通过。该结果用于开发并存在数据泄漏，只能作回归边界。

2026-09-05 `panel-split-3` 的合成回归新增 2D 视觉网格、游离小条带堆叠、无沟槽周期 Western 堆叠和不规则细文字拒绝。医生完整标注的 35 张 Figure 共 987 个 Panel；算法预测 806 个，IoU 0.50 下 TP/FP/FN=670/136/317、P/R/F1=0.831/0.679/0.747，IoU 0.75 下为 472/334/515、0.586/0.478/0.526。29 组关系有 16 组两端自动框均达到 IoU 0.50，自动裁剪逐对 matcher 命中 13 组。按“来源路径精确相同 + Finding 证据区域至少 50% 落入关系端点”复核，真实按案例批量基线是 11/29；早期 6/29 因比较临时 Panel ID 漏计。`panel-sift-fallback-1` 后为 13/29（新增 `eval-012-source-005-green`、`eval-032-source-001-red`，无丢失），17 案例总耗时约 134.97 -> 143.86 秒。5 个会触发该路径的已复核阴性小案例结果为 `0、6、2、9、6`，没有新 Finding ID，32 个医生已确认误报 ID 均未重现；113 个 Panel 的 `eval-097` 不进入该路径，实跑仍为 17 条并抑制 624 条。`panel-candidate-budget-1` 将阳性候选 1,138 -> 95、28 个阴性案例冻结结果 2,077 -> 296。全量 pytest 202/202、Ruff、格式、`pip check` 和 `git diff --check` 均通过。该批参与开发，不能作为独立 test。

2026-09-05 `panel-split-4` 增加“相邻条带被形态学合并时仍从原始轮廓恢复”的合成回归，并保留密集条带展开上限。35 张、987 个人工 Panel 上预测 857 个，IoU 0.50 TP/FP/FN=730/127/257、P/R/F1=0.852/0.740/0.792；IoU 0.75 为 505/352/482、0.589/0.512/0.548。29 组中 21 组两端自动框均达到 IoU 0.50，直接逐关系 matcher 命中 15/29；17 个阳性案例批量扫描为 950 个 Panel、107 条候选、约 153.92 秒，稳定区域口径命中 14/29。28 个已复核阴性案例为 3,370 个 Panel、预算后 328 条候选，较旧 296 条增加 32 条；32 个医生确认误报区域模式中同样 3 个在修改前后存在，没有新增已知模式。全量 pytest 203/203、Ruff、格式、`pip check` 和 `git diff --check` 均通过。测试集参与了本轮修改，所有结果均是 validation/regression。

2026-09-05 `panel-sift-fallback-2` 在超过 64 个 Panel 时增加跨原始 Figure 的分组 SIFT 短名单，并新增每 Figure 对上限、全局短名单上限和同 Figure 排除测试。17 个阳性案例、950 个 Panel 的真实按案例命中由 14/29 增至 15/29，只新增 `eval-006-source-003-blue`、无丢失；共 108 条结果、约 156.24 秒。28 个已复核阴性案例的完整试验仅新增 1 条同 Figure Western 行候选；加入跨 Figure 约束后该案例定向重跑恢复原结果，差异审计确认最终集合保持 328 条，32 个医生确认误报区域仍是修改前后的同 3 个、无新增。全量 pytest 205/205、Ruff、格式、`pip check` 和 `git diff --check` 均通过。该批参与策略选择，只作 validation/regression。

2026-09-05 `generic-image-local-5` 增加细长低色度条带行的固定对称边框归一化及高阈值合成回归。17 个阳性案例、950 个 Panel 的真实按案例命中由 15/29 增至 16/29，只新增 `eval-017-source-001-red`、无丢失；共 109 条结果、约 151.10 秒。28 个已复核阴性案例共 3,370 个 Panel，仍为完全相同的 328 个 Finding ID；分层拆分指标不变，自动拆分裁剪 matcher 为 16/29。本批参与策略选择，只作 validation/regression。

2026-09-06 为上述细长条带回归增加 Windows 稳定性约束：测试人为给纵轴翻转综合分加入 `5e-6` 平台偏差，要求边框归一化仍按固定顺序选择近似同分的允许变换，并继续满足原灰度、高频、梯度和置信度阈值；另一条回归要求显著更优的真实垂直翻转仍被拒绝。macOS ARM64/Python 3.12.14 下全量 pytest 213/213 通过，Windows 结果以最新 CI 为准。

2026-09-05 `generic-image-local-6` 有界横向子集试验及反馈闭环：医生将初始新增的 4 个阴性候选全部判为 `not_duplicate`，备注均为“排版相似，细节完全不一致”。加入 192×48 行列残差相关至少 0.88、对齐偏移不超过 6 的门禁后，17 个阳性案例、950 个 Panel 仍为 111 条结果，Finding ID 集合完全不变，真实按案例命中仍为 18/29（0.6207），约 176.02 秒；分层 split@0.50 P/R/F1 仍为 0.852/0.740/0.792，自动拆分端点逐对 matcher 仍为 19/29。28 个阴性案例、3,370 个 Panel 由试验的 332 条恢复为 328 条，差异恰好只移除这 4 条且无新增，约 538.23 秒。全量 pytest 211/211、Ruff、格式、`pip check` 和 `git diff --check` 通过。该批参与阈值开发，仍不计算独立 Precision/F1。

2026-09-05 阴性校准抽样进一步明确口径：最终 328 条结果中 `image.pathology.same_region_different_magnification` 的 1 条为正常关系，不进入疑似阳性复核；其余 327 条可再分发候选在同案例、同规则和同一无序原图对内，仅当 A、B 两侧区域 IoU 均至少 0.50 时聚类，最终为 326 簇。聚类只用于避免明显重复框，代表项标签不得自动传播到簇内全部候选。随后按 7 类规则和高/中/低置信度的 16 个非空分层确定性抽取 40 项、覆盖 26 个案例且每案例最多 2 项。ZIP 为 30,137,693 bytes，SHA-256 `0140581cdd6f9cef9b0c67640b34731f7d5d58ec65f72ca7ad7600c80c644a16`，只保存在 Git 忽略目录，压缩完整性通过。复核 ZIP 统一按 POSIX 相对路径字符串排序以消除 Windows 差异；macOS ARM64/Python 3.12.14 下全量 pytest 212/212、Ruff、格式、`pip check` 和 `git diff --check` 均通过。抽样结果用于定向误报优化，正式 Precision/F1 仍要求来源隔离 test 的完整计分标签。

### 分层图片评测

`scripts/build_panel_type_review.py` 从已冻结关系生成本地标注页面。关系红框只用于定位需要标注的内容，不能直接作为 Panel 边界；算法蓝框默认隐藏。`targeted` 任务只标关系涉及的 Panel，因此未匹配算法框不得计 FP；只有 `complete` 任务标完本图全部 Panel 后，才能计算拆分 Precision。

`scripts/freeze_panel_type_feedback.py` 校验任务、原图哈希、边界、类型和关系端点后生成 `layered_image_ground_truth`。`scripts/evaluate_layered_images.py` 分别输出：人工 Panel 上的 AUTO 类型路由指标、Panel 一对一 IoU 0.50/0.75 指标、人工 Panel 裁剪上的 matcher 指标，以及自动 Panel 两端均达到 IoU 0.50 后的关系指标。同一 `source_group` 跨 split 会直接报错。

现有 29 组确认关系的预标页为 `tmp/wechat-paper-dataset/unseen-eval-2026-09-01/panel-type-review-2026-09-04-v4/index.html`，包含 35 张唯一 Figure、58 个端点和 55 个目视预标 Panel。20 张受限图片及 15 张可再分发图片均通过相对路径引用原批次资产，页面不自包含且不得打包外发。预标页初始为 0/35 人工确认；逐卡勾选后才会以 `complete` 导出，人工反馈未返回并冻结前，各层指标仍为 `N/A`。

远程复核必须显式使用 `scripts/build_panel_type_review.py --remote --zip ...`。该模式复制许可元数据明确标为可再分发的资产；受限资产不得进入目录或 ZIP，改由页面使用 `official-assets.json` 中的 HTTPS 官方图片地址并提供原文章链接。当前远程包共 35 个任务：15 张图片内置、20 张仅联网引用；20 个直链均以有限范围请求验证为 HTTP 206。ZIP 19 个条目完整，15 个内置文件哈希恰好等于许可集合，与 20 个受限文件哈希交集为 0。

2026-09-04 新增定向测试，覆盖本地受限资产不复制、远程包受限资产只链接、远程包仅复制可再分发资产、预标草稿载入但不冒充已确认标签、目标到人工 Panel 的关系冻结、完整标注的拆分 Precision、`targeted` 标注不误计 FP、真实 AUTO 路由入口和 `source_group` 跨 split 拒绝。实际草稿经冻结工具做临时结构校验：35 张图、55 个 Panel、29/29 组关系均可解析，正式冻结文件未保留；浏览器验证 35 张图片全部加载、预标框和类型显示正常，初始进度 0/35，勾选与取消后进度 1/35 -> 0/35。全量 pytest 196/196、Ruff、格式检查、`pip check` 和 `git diff --check` 均通过。

2026-09-05 医生反馈的 35 个任务全部为 `complete`，原始 988 个 Panel 覆盖 58/58 个关系端点。`scripts/normalize_panel_type_feedback.py` 保留原反馈，以审计记录裁回 47 个负坐标或越界框、移除 1 个与 Western 框 96.7% 重合且类型冲突的病理重复框，并按医生补充将第 23、30 张从 `targeted` 改为 `complete`；冻结结果为 35 张完整 Figure、987 个 Panel、29/29 组关系。拆分共预测 493 个 Panel：IoU 0.50 的 TP/FP/FN 为 326/167/661，P/R/F1 为 0.661258/0.330294/0.440541；IoU 0.75 为 195/298/792 和 0.395538/0.197568/0.263514。人工 Panel 上 AUTO 类型 exact accuracy 为 0.389058；逐类召回为荧光 0.927536、通用 0.352227、病理 0.281022、Western 0.392947，本批没有 Dot blot 真值。人工 Panel 裁剪 matcher 命中 12/29，召回 0.413793；自动拆分两端 IoU 0.50 后仅 4/29 组可用、最终命中 3/29，召回 0.103448。关系集全为阳性，不能据此解释关系 Precision；本批参与过开发，只作 validation/regression。新增规范化测试后全量 pytest 197/197、Ruff、格式检查、`pip check` 和 `git diff --check` 均通过。

2026-09-03 阴性侧先生成 32 组误报校准包，而不是要求一次复核全部 4,588 条候选。包从 61 个可评阴性案例、59 个有候选案例中，仅对 1,852 条可再分发正式图候选按 7 类规则和高/中/低置信度分层，并以每案例最多 2 条的确定性抽样覆盖 28 个案例；2,736 条受限制正式图候选不嵌入可分享包。ZIP 共 172 个文件、24,826,687 bytes，SHA-256 为 `39e94489ff30085ab24f4873c1d2e543e84978eca70d3a8fe163c48fe8be457a`，压缩完整性通过。浏览器实测 32 卡、96 个反馈控件、128 张证据图且 0 加载失败，选择结果刷新保持、完成筛选和控制台检查均通过。该抽样用于定位误报模式和决定下一轮扩样，因案例均衡且排除受限制资产，不能单独计算或外推全量 Precision/F1。

首个真实 GPU 验证环境为 Windows 10/11 + NVIDIA GeForce RTX 3080 Ti。当前 `0.1.0a12` 只探测 GPU/驱动/OpenCV CUDA 状态并运行 CPU，不进行 CUDA 计算。RTX 实测应先以当前 CPU 后端生成图片和 Excel 基线；后续 GPU 后端加入后，用同一输入、参数和算法版本比较候选集合、阶段耗时、峰值显存及强制 CPU 回退。macOS 开发机和普通 GitHub Windows runner 可以验证接口、探测失败隔离、CPU 路径和打包，但不能替代真实 NVIDIA CUDA 验收。

当前仅有候选索引微基准：macOS ARM64/Python 3.12.13 中，为 2000 个合成图片项各生成 96 个随机 ORB 形状描述子，单独建立局部倒排候选索引用时约 2.9 秒，进程峰值 RSS 约 304 MiB，且未产生随机候选。该数字不包含图片解码、ORB 提取和几何精查，不能替代真实图片端到端性能验收。

Excel 合成微基准：同一环境中，20,000 个不同 11 位随机整数的数字片段检测约 1.6 秒并产生约 53,000 条候选；近似/序列高级规则约 0.8 秒并产生约 8,000 条候选，进程峰值 RSS 约 225 MiB。默认 4 位会自然产生大量低风险结果，该数字用于暴露结果膨胀风险，不代表真实实验表格分布或端到端报告时间。

`excel-advanced-2` 额外压力检查在同一环境中直接构造 80,000 个数值单元格（10,000 行 × 8 列），新结构/运算/统计规则约 3.3 秒完成，进程最大 RSS 约 95 MiB，并因人工数列中的精确十倍关系达到单次运算规则 300 条上限。该数字不含工作簿读取、旧片段/近似规则和报告生成，只用于验证实现不会对数万行直接执行无界两两计算。

Western blot 合成微基准：同一环境中，500 张 180×360 随机背景/随机条带图片提取出 542 个面板候选，特征提取约 2.3 秒；分段索引召回约 31,900 对候选并完成验证约 0.5 秒，进程峰值 RSS 约 101 MiB，最终产生 1 条低风险候选。该集合是专门观察候选膨胀的人工压力数据，不包含文件读取、通用图片算法，也不代表真实 Western blot 的准确率或端到端速度。

荧光/病理合成微基准：同一环境中，300 张 180×240 随机稀疏荧光图提取 290 个有效页面约 1.0 秒，候选索引与验证约 0.1 秒；150 张 240×240 随机染色形态图提取 279 个有效组织窗口约 0.7 秒，候选索引与验证约 0.02 秒，两组无关随机输入均未产生结果。该测试在内存中直接传递像素，不包含文件解码、通用算法或 GUI/报告；真实组织密度、重复视野和文件名分组可能增加候选数，不能用作准确率或端到端性能承诺。
