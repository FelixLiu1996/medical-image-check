# 项目状态

最后更新：2026-08-29

## 已完成

- 多轮产品需求澄清。
- 第一版 PRD 初稿。
- 算法、架构、数据模型、UI、测试、发布和路线图初稿。
- 跨窗口交接与文档同步规则。
- 配置 GitHub 远程仓库 `origin`。
- 建立需求与治理文档的 Git 基线。
- 用户确认 PRD 0.1 并授权初版开发。
- 创建 `codex/v0.1-foundation`。
- 通过 ADR-0002 确定 Python/PySide6 Essentials 模块化单体。
- 建立领域、引擎、基础设施、服务和 UI 分层。
- 实现图片文件 SHA-256 重复检测。
- 实现 Excel 完整数值、数值行重复和四种表格格式读取。
- 实现项目清单原子保存与版本检查。
- 建立 Ruff、pytest 和 Windows/Linux GitHub CI。
- 将项目新建、打开、保存、另存为和未保存提醒接入桌面 UI。
- 项目清单升级到版本 2，保存并恢复最近扫描结果、提示和报告路径，兼容读取版本 1。
- 实现包含概览、查重结果、扫描提示和项目输入的基础 Excel 报告。
- 引入 NumPy 2.5.2 和 opencv-python-headless 4.14.0.94，并登记许可证。
- 实现解码像素指纹、跨格式像素重复、多页 TIFF 和整体感知近似候选。
- 实现旋转/翻转指纹、分段候选索引及标准化缩略图验证。
- 通过 ADR-0003 确定 Windows Alpha 免安装包使用 `pyside6-deploy`/Nuitka standalone。
- 建立 Windows 免安装 ZIP、打包冒烟、SHA-256 和第三方许可证收集工作流。
- 实现 ORB 局部描述子候选索引、双向比率匹配及 RANSAC 仿射/单应验证。
- 实现裁剪、大图包含小图和两张裁剪图部分重叠检测，输出两侧区域与几何参数。
- 在 GUI 中增加双图并排证据预览、匹配矩形和几何证据摘要。
- 实现 Excel 连续数字片段倒排分组，默认 4 位、可调 3–12 位并按长度分级。
- 实现 Excel 近似值分层窗口和接近零绝对容差。
- 实现连续列完全重复、固定倍数、固定偏移、固定整数和与固定整数积关系。
- 项目清单升级为版本 3 并兼容版本 1/2，保存片段阈值与递归 JSON 证据。
- GUI 增加 Excel 证据摘要，Excel 报告增加逐行“数值证据”工作表。
- 实现 Western blot 明暗极性背景归一化、横向条带连通区域和面板候选提取。
- 实现受限分段指纹候选索引，并联合条带结构、排列几何、掩膜和去条带背景纹理验证。
- 实现跨图曝光/水平翻转及同图面板 Copy-Move 候选；完全重复图片不重复生成 Western 结果。
- 增加项目级 Western blot 单条带敏感检测开关，默认关闭且单条带结果固定为低风险。
- GUI 增加 Western blot 匹配框和证据摘要，Excel 报告增加“图像证据”工作表。
- 项目清单升级为版本 4 并兼容版本 1/2/3，保存单条带开关。
- 实现荧光文件角色识别、有效颜色通道分解、分段候选索引、小范围平移配准，以及结构/前景掩膜/归一化互信息联合验证。
- 将荧光单通道与 Merge、不同通道同视野分类为正常关系；同通道或合并图高一致分类为疑似复用。
- 实现病理光密度组织掩膜、染色相对稳定形态、多尺度重叠组织窗口和受限分段候选索引。
- 将病理同区域不同倍率分类为正常关系，同倍率高一致组织区域分类为疑似复用；每个图片页对只保留最佳证据。
- GUI 增加荧光/病理中文证据摘要及组织区域裁剪预览；Excel“图像证据”扩展为三类专项统一结构。
- 三类专项继续共享单次图片解码，并增加候选桶、每页特征和页对结果上限等工程保护。
- 实现线程安全的扫描暂停、继续和安全取消；在文件、算法阶段和候选批次设置协作式检查点，取消不保存部分结果并恢复扫描前完整结果。
- GUI 增加暂停/继续/取消按钮，关闭正在扫描的窗口会先确认并安全取消。
- 通过 ADR-0004 确定单文件 HTML 与 ReportLab PDF 报告方案。
- 增加单文件 HTML 报告，内嵌 CSS、JavaScript 与 PNG 图像证据，支持文本搜索和风险筛选且不加载网络资源。
- 增加中文 A4 PDF 报告，包含总览、完整结果表、图像证据、扫描提示和页码；优先嵌入 Windows/macOS/Linux 系统中文字体。
- GUI 统一为三格式报告导出，并增加聚焦匹配区域和复制证据摘要。
- 登记 ReportLab、Pillow、charset-normalizer 许可证，并将实际 wheel 许可证收集加入 Windows portable 工作流。
- 将打包程序 `--smoke-test` 扩展为实际生成并校验 Excel、HTML、PDF 三种临时报告，防止报告依赖漏打包。
- 实现 Excel 可配置相对/绝对容差、单次完整值加减乘除、连续加减乘除目标、连续数值片段、顺序打乱、少量修改和连续二维区域规则。
- 实现 Theil–Sen 稳健线性关系，以及均值/标准差异常一致和标准化分布相似的固定低风险统计提示。
- GUI 增加 Excel 高级参数区和扩展证据摘要，参数随项目保存并写入 Excel/HTML/PDF 报告。
- 项目清单升级为版本 5 并兼容版本 1–4；Excel 参数变化使旧扫描结果失效。
- 将单页混合界面重构为现代化中文首页、图片查重工作区和数据查重工作区。
- 图片/数据工作区分别过滤直接文件、文件夹内容、参数和结果证据；项目操作降为可选辅助能力。
- 扫描服务增加 `all/image/data` 模式，单一工作区会跳过另一类检测器且保持旧调用兼容。
- 根据首轮 Windows Alpha 反馈重构 Excel 候选降噪、强度排序和跨工作簿/工作表公平选取；过滤全零、常量、身份运算和零乘积等低信息量关系，避免前序工作表占满规则上限。
- 数据结果改为“上方候选列表 + 下方左右原表上下文”，支持 xlsx/xlsm/xls/csv 有限窗口只读预览、行列标题和命中单元格黄色高亮。
- 增加 Dot blot 斑点阵列专项，支持裁剪、缩放、灰度/对比度变化后的排列复用候选；通用、Western blot、Dot blot、荧光和病理可自动路由或由用户明确指定。
- 收紧自动模式的 Western blot/病理准入，并在专项证据存在时折叠同一图片对的通用重复结果，减少跨类型错误解释。
- 项目清单升级为 schema 6，保存图片内容类型并兼容迁移 schema 1–5；开发版本升级为 `0.1.0a10`。
- 结果区增加可选的“准确、误报、正常关联”标记、清除标记和复核状态筛选；不设置审核任务、完成率、审核人或强制备注。
- 反馈复用 schema 6 的 `Finding.review_status` 随项目保存；同一算法版本的稳定结果重新扫描时按结果 ID 继承。
- 增加 Excel/JSON 算法反馈清单，只导出已标记结果的版本、规则、位置和结构化证据，不复制原始文件。
- 功能分支命名统一为 `<agent-name>/<feature-name>`，例如 `codex/lightweight-review-feedback`。
- 增加 performance schema 1，记录扫描总时长、有效运行时长、暂停时长以及图片/Excel 分阶段耗时和处理项数。
- 增加 NVIDIA 与 OpenCV CUDA 只读探测；探测失败自动保留 CPU 路径，不阻断任何核心查重。
- 项目清单升级为 schema 8 并兼容 schema 1–7；最近扫描的性能画像和复合图子面板选择可随项目保存，旧项目保持可读。
- GUI 增加 JSON 性能诊断导出，诊断不包含原始数据、输入路径、查重位置或结构化证据。
- 通过 ADR-0005 确认先画像再选择 GPU 后端，以 RTX 3080 Ti 为首个参考环境而非最低配置。
- Excel 读取增加最近列标题、标题行和只读公式引用元数据；序列按表格区块分段，明确编号段不进入数值规则。
- 将同排公式或明确 `rescaled`/归一化表头支持的固定换算分类为正常派生关系，不再作为高风险异常候选。
- 数据工作区增加“重点候选（推荐）/全部线索”范围切换；默认队列跨规则归并并限制为最多 50 条，同时保留全部线索、正常关系和报告证据。
- Excel、HTML、PDF 报告结果表增加候选层级，HTML 增加层级筛选；ADR-0006 记录完整线索与有界重点候选并存策略。

## 当前阶段结论

- Western blot 上一阶段已通过 Windows/Linux CI 和 Windows portable 构建及下载校验。
- 荧光/病理专项与 GUI/报告已推送，并通过本阶段 Windows/Linux CI、Windows standalone 编译、程序启动冒烟、ZIP 组装和工件上传。
- 扫描控制、三格式报告和证据交互已推送；远程发现并修复 Windows OpenCV 测试夹具直接写中文路径的问题，产品读取链路不受影响。
- 本阶段 Windows/Linux CI、Windows standalone 编译、打包程序三报告冒烟、许可证收集、ZIP 组装和工件上传均已通过。
- Excel 已确认数值规则的全局扫描 Alpha 基线已补齐；自动识别实验组与手动框选尚未进入当前 GUI。
- 首轮反馈的两组问题已完成本地修复：13 个真实工作簿候选由 9,864 条降至 1,802 条且已知阳性保留；4 张同源 Dot blot 自动模式识别全部 6 组两两关系，未再输出 Western blot/病理串类结果。真实样例未进入仓库，样本量不足以作为准确率承诺。
- 轻量反馈已合入并推送 `main`（`2d9ea57`），开发分支和 main CI 均通过。
- Dot blot 已升级为局部背景弱斑点候选、任意方向阵列、3–8 个近连续局部子集和逐斑点局部图像联合验证；同一页对只保留最佳结果，相同排列但局部内容不相容的合成负例已被拒绝。
- 增加本地 Dot blot 逐对标签清单评测器，输出 TP/FP/FN/TN 等指标并校验同一来源组不跨数据集；不下载、复制或提交真实图片。
- 当前开发版本为 `0.1.0a17`，扫描算法为 `generic-image-local-2+western-blot-2+dot-blot-3+fluorescence-1+pathology-2+panel-split-1+excel-advanced-4`；当前计算后端仍固定为 CPU，尚未启用 CUDA。
- 当前开发分支已整合 `codex/composite-panel-splitting` 的批次级可选复合图拆分：规则视觉岛自动候选、扫描前原图框选预览、默认全选/逐项取消、普通单图整页回退、schema 8 项目恢复，以及临时无损裁剪到原图路径/页码/证据坐标的回映。第一版尚不能拖拽调整、重新切割、合并或命名。
- Excel 工程优化本地回归已完成：正常 `rescaled` 公式列不再进入重点异常队列，默认重点候选由约 1,800 条全部线索收敛为 50 条，且三类已知阳性仍在重点队列。该结果是工作量控制，不代表 50 条均为真实异常。
- 最新 Excel 阶段 Windows/Linux CI 和 Windows portable 已通过；打包程序三报告冒烟、许可证收集、ZIP 组装和工件上传均成功。
- Dot blot `dot-blot-2` 提交 `9cd400c` 已推送开发分支，CI 与 Windows portable 均成功；用户已确认快进合入 `main`。
- 已在 `codex/image-benchmark-v1` 建立通用医学图片逐对本地评测器，支持同图/跨图区域、按类型运行、Western 单条带开关、来源组隔离及分类型指标；真实图片和清单继续只存在于 Git 忽略目录。
- 第一版本地 validation 整理出 39 对：26 对阳性、13 对阴性，覆盖 Western blot、免疫荧光、普通病理和细胞明场；Dot blot 暂缓。去除可见彩框并优先换用未画框原图后，`western-blot-1` 基线为 11 TP、1 FP、15 FN、12 TN；`western-blot-2` 定向修正后为 16 TP、0 FP、10 FN、13 TN。该结果只用于同集回归，不是正式准确率。
- 医学人员已逐对确认首轮 Western blot 的 5 个漏报均为重复，唯一误报（阴性论文第 15 页 G/H 的 beta-actin）为不重复；`western-blot-2` 在这 11 对 Western validation 上为 7 TP、0 FP、0 FN、4 TN。此前确认的 Casp11/NEDD4 目标条带及其 beta-actin 对仍被正确排除。
- 首篇 Figcheck 数据链路试点已完成公开正文和 33 张实际图片获取、目标论文 DOI/PMID/PMCID 与正式撤稿核验、37 处聚合声明拆解、CC BY-NC 许可证判断及主文 Figure 1–7 获取；真实材料和本地清单仅在 Git 忽略目录。用户已确认首轮只处理正式全文主文 Figure：当前范围 31 处，Supplement 相关 6 处延后且不阻塞。`docs/WECHAT_PAPER_DATASET_FEASIBILITY.md` 同时保留后续 Supplement 获取和企业 MDM/DLP 阻塞的合规流程；尚未修改产品范围或实现抓取/训练工具。
- 首篇案例已将公众号 Figure 7 彩色框配准到 PMC 无框原图，生成本地 10 组、25 个候选区域的编号总览和复核图。医学沟通后确认该案例只适合验证获取、配准、溯源和复核图生成，不适合作为独立医学 validation/test：公众号结论较完整，且缩放、旋转、翻转后的荧光图仅靠肉眼难以可靠复核。
- 公众号样本复核流程已优化为职责分离：`source_reported` 只表示候选来源；系统生成具体 A/B 后进入 `source_pair_confirm`，未生成具体映射则保留为 `mapping_not_generated`，`novel_candidate` 作为医学复核最高优先级，`insufficient_evidence` 等待更高质量原图；公开来源结论不得冒充独立医学标签。
- 用户提供的 Word 清单已解析为 42 个唯一公众号案例，并明确要求全部抓取、不得只选少量试点。第一阶段获取已完成：42/42 篇公开正文均为 HTTP 200 且存在完整 `#js_content`，191/191 个唯一正文图片 URL 下载成功；原始 HTML、正文、图片和逐案清单约 167 MB，全部位于 Git 忽略目录。
- 42 个案例的全量后续流程已完成到交互复核包：解析 64 篇去重论文，49 篇有 PMC 全文，47 篇实际含主文 Figure，共取得 306 张正式正文图且 0 张下载失败；公众号证据图没有进入算法扫描。本地通用整图/局部几何规则形成 8 条未确认 `novel_candidate`，分布在 7 个案例。
- 191 张公众号证据图已完成本地 OCR，并按同色彩框分组、以 SIFT/单应性配准回正式全文；共检测到 49 个彩框关系组，27 组达到保守阈值并形成 `source_pair_confirm`，分布于 11 个案例。其余 22 组继续作为系统待办，不要求医学人员手工定位。
- 已生成覆盖 42 个案例和 66 张任务/状态卡的离线交互 HTML；实际需医学确认 35 条，包括 27 条 `source_pair_confirm` 和 8 条 `novel_candidate`。另有 18 条 `mapping_not_generated`、13 条 `insufficient_evidence` 仅显示处理状态，无反馈按钮。页面支持搜索、筛选、正式图/来源图查看、本地保存和反馈 JSON schema 3 导入导出；浏览器已验证案例渲染、图片加载、反馈保存和最终初始状态 0/35。真实材料和交互包继续只在 Git 忽略目录。
- 医学协作者反馈“仅并排整图看不出算法命中区域”后，8 条程序候选均已补充范围证据：7 条局部几何候选生成左右红框整图与局部放大，1 条感知候选明确为整图比较；页面同时展示关键点、几何内点、旋转和尺度，并在顶部解释三类任务及对应反馈。首例红框显示算法实际命中下方统计图而非上方组织图，复核人可据此直接判定版式误报。
- 医学协作者进一步明确：公众号图片本身已用文字和同色框说明重复关系，系统应先读取并生成具体 A/B，不能让复核人逐行手工录入。页面现为每条 `source_pair_confirm` 展示来源同色框两端、论文/DOI/Figure/Panel、正式全文蓝框位置和局部证据，再让医学人员确认映射与重复结论。第 22 例已形成两组具体关系，并单独提示公众号文字 DOI 与图像内容配准结果的冲突。
- 已收到并校验首轮医学反馈 JSON：35/35 个稳定 ID 完整、无多余项。原始反馈只读固定后，规范化为 17 条可用来源正例、1 条来源负例、8 条算法困难负例、8 条需修正映射和 1 条需补证据；4 条按钮/书面理由冲突均按理由保守处理，没有覆盖原始记录。
- 8 条算法候选均被医学反馈否定并固定为 hard-negative validation；当前基线为 5 FP/3 TN，主要误报来自统计图、折线/柱状图、组合排版和整图版式。18 条可计分来源关系的基线为 9 TP/0 FP/8 FN/1 TN。两组结果都只用于开发回归，不能作为独立准确率。
- 已按反馈完成映射返工：第 1 例把错误的同图配对拆分、去重为 5 条正式全文跨论文关系，并删除 1 条“局部标记 vs 同 Panel 整体”的无效自比较；第 25 例两条 A 端 Panel 从 Figure 6L 修正为 Figure 6K/Figure 6I；第 34 例两条只存在于公众号后处理/伪彩图的关系因正式主图没有直接对应区域，被保留审计但排除出正式主图训练数据。
- 第 34 例剩余不确定条带已增加正式图定位、对称裁边、共享对比度、平移对齐、边缘和差异证据。裁边后仅平移的辅助相关度约 0.977，但页面明确要求医学判断，不把分数当标签。已生成 8 项二次复核包（7 个定位 QA + 1 个增强证据判断），36/36 张页面证据图片加载正常，选择、进度和刷新恢复已验证且无浏览器控制台错误。
- 已收到并校验二次反馈 8/8 项：7 个纠正后的正式图定位全部确认正确，最后 1 个 Figure 5A 条带关系确认高度重复。最后一条原本已由公众号紫色框报告，本轮属于“公开来源报告 + 增强证据 + 医学质控”，不是算法新增发现。
- 两轮反馈初步收口为 34 条 validation；2026-08-31 对来源图彩框四边直线连续性复核后，排除病理组织 #02（Azan 洋红染色被误作标记框）。当前有效集为 33 条：25 positive、8 negative；原始反馈不改写，排除原因单独留存审计。
- `generic-image-local-2` 按 Western blot、病理、荧光和通用图片路由后，当前 33 条同集开发回归为 25 TP、0 FP、0 FN、8 TN。4 条病理关系由自动组合中的局部结构技术检测器补充召回，但用户界面和报告不再将其误称为 Dot blot。该批数据参与过诊断与调参，只能用于开发回归，不能作为独立 test 或对外准确率。

## 下一步

1. 由医学人员优先复核首轮阳性对，特别是只依据来源彩框、局部过小或缩放较大的案例；未确认项不得进入 test。
2. 锁定当前 Western 阈值，补充来源隔离的新 Western 阳性和相似内参阴性，验证窄条带行回退能否泛化；在新样本到来前不继续围绕这 11 对调参。
3. 继续补充来源隔离的 Western、HE/IHC 和免疫荧光困难阴性及真实阳性；Dot blot 按用户决定暂缓。
4. 建立从未参与调参的 test 集后再报告准确率；RTX 3080 Ti 性能诊断与算法准确性工作保持分离。
5. 冻结 `generic-image-local-2` 和当前 33 条开发回归，不再围绕同集继续调阈值；优先收集按 `source_group` 隔离的新论文建立独立 test。22 个尚未恢复的彩框关系组继续由系统处理，13 条 `insufficient_evidence` 在取得许可明确的正式主图前不计分。

## 阻塞或待定

- 第一批 validation 已建立，但部分阳性仍只有来源标记，缺少逐对独立医学确认；独立 test 集尚未形成。
- 算法准确率与阈值仍待更多来源隔离样例校准；当前 39 对结果不得对外宣传为准确率。
- GPU 性能画像已经实现，但 GPU 后端、依赖选择和加速效果仍待 RTX 3080 Ti 实测后确认；当前版本不得宣传为 CUDA 加速版。
- RTX 3080 Ti 参考机的驱动版本、OpenCV CUDA 状态和真实性能诊断尚未回传。
- 是否在 Excel、HTML、PDF 报告中增加左右原表上下文及黄色命中高亮，待与试用用户确认；当前只有 GUI 支持该预览，报告仍输出结构化位置和数值证据，暂不开发此增强。
- 当前通用图像阈值已通过合成数据和 33 条同集开发回归，但尚未通过来源隔离的独立 test，不能作为准确率承诺。
- 当前 Western blot 只对明显横向条带行形成自动面板，阈值仅通过合成数据验证；复杂排版、文字标签、弱条带和任意擦除/拼接仍可能漏检或误报。
- 当前荧光基线依赖可分离颜色通道或文件名角色；未读取显微镜元数据、未建立实验组语义，不同通道结构差异大时可能漏检。
- 当前病理基线只支持普通静态图片；未支持全切片格式和连续切片语义，强染色差异、非规则尺度或任意旋转仍可能漏检。
- 当前 Excel 片段/近似/运算/结构/统计阈值只通过合成数据和一组真实工作簿回归；降噪与公平选取能减少无意义候选，但默认 4 位数字片段仍可能产生较多低风险线索。规则仍有全局和分作用域上限，结果不能代替人工判断。
- Excel 派生列语义只覆盖可解释的同排引用和有限表头关键词，不执行任意公式，也未理解实验组含义；重点队列只是排序和配额，全部线索中仍可能有较多偶然重复。
- 暂停/取消是进程内协作式控制；单个大文件解码或验证批次结束前可能延迟响应，关闭软件后不能恢复未完成任务。
- HTML 最多内嵌前 120 条图像证据，PDF 最多内嵌前 40 条；全部结果仍保留在结果表和 Excel 报告。
- PDF 已在 macOS 嵌入中文字体并完成渲染/文本层验证；Windows 系统字体发现、不同阅读器和打印仍待 portable/实机验收。
- Windows 免安装包已通过 Actions 构建与冒烟，仍需干净 Windows 10/11 实机人工验收。
- 公众号论文样本链路已完成 42 个案例的正文资料、64 篇论文身份、306 张正式主图、来源关系及两轮医学反馈；彩框有效性复核后当前保留 33 条 validation。当前阻塞转为 22 个未恢复彩框关系组的系统映射、13 个缺正式主图案例的合规资产补全，以及用于验证 `generic-image-local-2` 泛化效果的来源隔离新 test；当前同集回归不能作为独立准确率。真实正文、图片、交互包和反馈均未提交仓库。

## 明确未开始

- Windows 安装程序和正式 Release
- 通用单图 Copy-Move、复合图手工调整边界/重新切割/合并/命名、荧光实验组语义和病理连续切片语义
- Excel 自动识别实验组和手动框选扫描区域
- 历史库和完整项目包
- NVIDIA CUDA 计算后端及 CPU/GPU 一致性验证

## 最新验证

- 2026-08-31 Windows/Python 3.12.10：`ruff format --check`、`ruff check`、`pip check` 和 `git diff --check` 通过；`QT_QPA_PLATFORM=offscreen` 下 pytest 140 项全部通过。`0.1.0a17` wheel 构建通过，SHA-256 为 `deb893e0386bd7737be2b9eb2ecd49e8ffc089138918ccbec7c20bf436dd2b7f`。来源框复核后的 33 条按医学类型路由开发回归为 25 TP、0 FP、0 FN、8 TN；该批样本参与过开发，不是独立准确率。
- Windows 本地 Python 3.12.10；Ruff 检查与格式检查通过，`pip check` 通过。
- `QT_QPA_PLATFORM=offscreen` 下 pytest 123 项全部通过；包含通用图片逐对评测、窄条带行缩放/翻转和 Western 低覆盖细长局部证据过滤回归，并保留既有 Dot blot、Excel 和 GUI 测试。
- 本次 `generic-image-local-2` 在 macOS ARM64/Python 3.12.13 完成最终验证：`ruff format --check`、`ruff check`、`pip check`、`git diff --check` 均通过，`QT_QPA_PLATFORM=offscreen` 下 pytest 129 项全部通过；新增白底图表抑制、小区域密集结构/SIFT、无关小条带压力和 GUI 证据解释回归。
- `panel-split-1` 独立开发分支在合入前 pytest 132 项通过；覆盖复合图识别、单图回退、多页 TIFF 映射、部分勾选、损坏文件隔离、schema 7 迁移、临时裁剪清理、原图坐标回映和预览弹窗。
- `panel-split-1` 与 `generic-image-local-2` 合入后，macOS ARM64/Python 3.12.13 下 `ruff format --check`、`ruff check`、`pip check` 和 `git diff --check` 通过，`QT_QPA_PLATFORM=offscreen` 下 pytest 138 项全部通过；后续排除无效病理组织 #02，当前 33 条按医学类型路由回归为 25 TP、0 FP、0 FN、8 TN。
- 对 `official-papers` 中 299 张正式全文图片完成无标签拆分冒烟：11 张整页回退，288 张生成多个候选，共 2,836 个候选、单图最多 46 个，约 8.56 秒。该结果只证明批处理可运行，也提示部分图片可能过拆；没有人工面板边界时不能计算拆分准确率。
- `0.1.0a16` wheel 已构建并确认包含面板领域模型、拆分服务和 GUI；SHA-256 为 `aeb82869c85cf9c5c75ca84eb5ec077282cedacca6a3bcb6ee9e11487b19b2c3`。
- Qt offscreen 启动通过
- `pyside6-deploy --dry-run` 配置解析通过
- 第三方许可证收集脚本本地通过
- Python wheel 构建与源码编译检查通过
- `0.1.0a11` 轻量反馈结果区已通过 Qt offscreen 截图检查；开发分支 CI run `32926326818`、Windows portable run `32925292521` 及合入 main 后 CI run `32926991817` 均通过。
- `0.1.0a12` macOS ARM64 真实小图片扫描画像正确识别 CPU/无 NVIDIA 环境并输出阶段耗时；扫描规则和算法版本未改变。
- `0.1.0a12` Qt offscreen 1024×720 与 1280×900 布局检查、wheel 构建、打包程序三报告冒烟、许可证收集、源码编译和 `pyside6-deploy --dry-run` 均通过。
- 4 张真实同源 Dot blot 本地只读回归：`dot-blot-2` 自动模式下 6 组两两关系全部输出且没有其他专项串类，无读取问题，约 1.8 秒；样例未入库。
- Dot blot 等间距负例合成微基准：25 张独立四斑点图片提取 28 个阵列候选约 1.6 秒、验证约 2.3 秒，输出 0 条专项结果。
- Dot blot 开发分支提交 `9cd400c` 的 CI run `32989243824` 和 Windows portable run `32989243851` 均成功。
- 图片 validation 与 Western 改进提交 `49c99f1` 的 [CI run 33147098251](https://github.com/FelixLiu1996/medical-image-check/actions/runs/33147098251) 和 [Windows portable run 33147098252](https://github.com/FelixLiu1996/medical-image-check/actions/runs/33147098252) 均成功。
- 13 个真实 Excel 工作簿本地只读回归：1,775 条全部线索、22 条正常派生关系、50 条重点候选；已知连续片段/配对和/结构重复阳性均在重点队列，约 6.36 秒；修复前为 9,864 条。
- `0.1.0a13` PDF 候选层级列通过两页 A4 Poppler 全页渲染目视检查，无裁切、重叠或分页异常。
- `0.1.0a13` Qt offscreen 打包程序三报告冒烟、源码编译、wheel 构建和 `pyside6-deploy --dry-run` 均通过。
- `0.1.0a9` Qt offscreen 首页/图片/数据三页截图检查通过；900px 高窗口顶部导航稳定，工作区可滚动
- `medical_image_check-0.1.0a9-py3-none-any.whl` 隔离临时目录构建通过；本地打包冒烟与 `pyside6-deploy --dry-run` 通过
- 双入口 GUI 源码 CI Windows/Linux run `32862204165` 通过。
- Windows portable `0.1.0a9` run `32862205003` 完成 standalone、打包程序三报告冒烟、许可证收集、ZIP 组装和上传，总耗时 24 分 39 秒；工件 92.3 MB，artifact digest 为 `9507b3b0998fb51cde1e48bdd1da1a880735ffd38a1dbe1583a47652edd7fd23`。
- 2000 项×96 描述子的局部候选层合成微基准约 2.9 秒、进程峰值 RSS 约 304 MiB（不代表端到端扫描）
- 20,000 个 11 位合成数值：片段规则约 1.6 秒/53,000 条候选，高级关系约 0.8 秒/8,000 条候选，峰值约 225 MiB（不代表真实分布）
- 80,000 个合成数值单元格（10,000 行 × 8 列）：`excel-advanced-2` 新规则约 3.3 秒、最大 RSS 约 95 MiB，单次运算达到 300 条保护上限（不含读取、旧规则和报告）
- 500 张 180×360 随机合成 blot：约 2.3 秒提取 542 个区域，约 0.5 秒验证 31,900 对索引候选，峰值约 101 MiB，产生 1 条低风险结果（不代表真实分布）
- 300 张随机稀疏荧光图：约 1.0 秒提取 290 个有效页面，约 0.1 秒完成候选索引与验证，无关随机输入无结果（不含解码/通用算法）
- 150 张随机染色形态图：约 0.7 秒提取 279 个有效组织窗口，约 0.02 秒完成候选索引与验证，无关随机输入无结果（不含解码/通用算法）
- GitHub CI Windows/Linux 通过（run `32834918247`）
- Windows portable `0.1.0a5` 构建、打包冒烟和工件上传通过（run `32834918889`）
- 下载后的 portable ZIP 通过原始 `.sha256` 文件校验；共 133 个条目，主程序、Qt/OpenCV 运行库和许可证材料齐全
- 荧光/病理阶段 GitHub CI Windows/Linux 通过（run `32840344162`）
- Windows portable `0.1.0a6` standalone 构建、程序冒烟、ZIP 组装和上传通过（run `32840344121`）；Actions 工件 `MedicalImageCheck-windows-x64-portable` 为 85,370,794 字节
- 扫描控制、HTML/PDF 报告和 GUI 证据交互本地 74 项测试通过；PDF 两页中文样例通过 Poppler 全页渲染目视检查和 pypdf 文本层验证
- ReportLab、Pillow、charset-normalizer 的实际 wheel 许可证收集通过
- 扫描控制与三格式报告阶段 GitHub CI Windows/Linux 通过（run `32844253484`）
- Windows portable `0.1.0a7` standalone 构建和打包程序三报告冒烟通过（run `32844253505`，总耗时 22 分 18 秒）；Actions 工件 `MedicalImageCheck-windows-x64-portable` 为 92.1 MB，artifact digest 为 `63d43c1488b5798e28ceccc57185617c3e4ad4c688868658fdf8fc52e6068e74`
- Excel `0.1.0a8` 最新源码 CI Windows/Linux 通过（run `32849587649`）；Windows portable run `32849587674` 完成 standalone、程序三报告冒烟、许可证收集、ZIP 组装和上传，工件 92.3 MB，artifact digest 为 `4b395721340e286b9ae278c58c0ee5c5292ca2d68b1c9290c7bd6aa12cf21083`
