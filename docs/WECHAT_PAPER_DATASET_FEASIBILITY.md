# 公众号论文图片标注数据链路可行性与交接

- 状态：首篇试点已验证主要链路；后续 42 个案例已完成公众号资料、论文身份、主文正式图、初轮算法候选、两轮医学反馈及来源框有效性复核；当前有效 validation 为 33 条，新收集的 124 例已完成 post-unseal 全量回归并进入代表性候选医学复核；未进入正式训练阶段
- 日期：2026-08-28（2026-09-01 更新）
- 用途：记录首篇试点、42 案例全量处理、候选分流和医学反馈流程；Supplement 仅作为后续可选扩展

## 1. 目标与边界

本次讨论的目标不是把公众号或论文检索接入软件的在线查重入口，而是建设一条本地训练与评测数据生产链路：利用科研诚信类公众号文章中已经标出的疑似图片重复关系，定位对应论文及干净原图，形成可追溯的正负样本。

该方向尚未写入 `docs/PRD.md`，也没有开始实现。若后续要成为正式开发范围，必须先同步 PRD、ROADMAP、STATUS、CHANGELOG，并视技术与数据许可选择补充 ADR。

## 2. 已达成的核心结论

### 2.1 三类来源承担不同职责

- 公众号、PubPeer 等公开标注：提供真实重复案例、可疑区域、Figure/Panel 编号和变换关系，是标签线索来源。
- PubMed、Crossref：用于解析论文身份、DOI、PMID、作者、期刊和年份，不作为主要图片文件来源。
- PMC Open Access、出版社开放附件、BioImage Archive 等：在许可证允许时提供无红框、无箭头的干净论文图片或实验图像，是训练图片资产来源。

公众号不应仅被视为“选题线索”。像 Figcheck 这类文章会直接圈出重复图片、重复区域和细节，因此可以显著降低寻找真实阳性的成本。但公众号标注图不能直接作为模型输入，否则算法可能学习红框、箭头和相同压缩痕迹，而不是图像复用本身。

### 2.2 标注与干净原图必须分离

推荐处理方式：

1. 保留公众号正文和标注截图作为本地证据。
2. 从正文提取论文标题、DOI、PMID、Figure/Panel 和重复关系。
3. 从许可明确的原始发布来源获取干净 Figure。
4. 将公众号标出的区域重新映射到无标记原图。
5. 使用干净原图裁出样本，公众号截图只用于定位和追溯。

这与 `docs/IMAGE_PAIR_EVALUATION.md` 的现有约定一致：彩色标记框只能用于定位，不得成为程序命中的依据。

### 2.3 默认负例规则

考虑到逐对证明所有图片“绝对不重复”的成本不可接受，初期采用工程化闭世界假设：

- 公众号或 PubPeer 明确指出且能精确映射到干净原图的重复关系，先记为 `source_reported` 正例候选；完成坐标与证据质控后可成为“公开来源标注正例”，但不能写成“医学人员独立确认”。
- 未被公开标出的图片对默认作为负例，参与训练和初步评测。
- 标注范围不清楚，例如只说“Figure 3 有问题”但没有指出具体两个 Panel，则暂时标为 `unknown`，不能强行生成正负标签。
- 当算法命中某个默认负例时，只复核这个冲突样本；确认重复则改为正例，确认不重复则保留为困难负例，无法判断则移出计分集合。

默认负例在训练时按普通负类使用，不要求降低权重；但必须保留来源元数据，例如：

```json
{
  "expected": "negative",
  "label_source": "unreported_assumption",
  "review_status": "not_reviewed"
}
```

对外描述指标时必须明确：指标基于公众号及 PubPeer 的公开标注构建，未被公开标注的图片对默认视为负例。若算法发现此前未报道的真实重复，初始统计可能将其计为误报，完成复核和标签修正后再重新计算。

### 2.4 候选分流与医学复核优先级

医学人员不应重新从零判断公众号已经明确报告的全部案例。公开来源、算法和医学复核承担不同职责，应先分流再决定是否需要专家介入：

| 候选类型 | 定义 | 处理方式 | 医学复核优先级 |
| --- | --- | --- | --- |
| `source_pair_confirm` | 公开来源已经用文字或同色彩框给出关系，系统也已解析论文/Figure/Panel/区域并配准到干净原图 | 展示来源标注图、具体 A/B 端点、正式全文定位图和局部证据；医学人员只确认“映射是否正确”和“A/B 是否重复”，不从头抄录关系 | 高；逐对完成标签质控，但不能称为来源独立的 test |
| `mapping_not_generated` | 来源声称存在重复，但系统尚未可靠恢复具体 A/B 两端，或仍有彩框关系组未达到 OCR/配准阈值 | 保留为系统待办，继续改进 OCR、Figure/Panel 解析、局部配准和 Supplement 资产；不得把手工定位成本转嫁给医学人员 | 不复核；待系统生成具体关系后再进入复核 |
| `novel_candidate` | 本地算法在正式原图中发现相似或复用候选，但公开来源没有报告 | 从默认负例集合中暂时移出，保守标为 `unknown`；生成完整证据包后优先人工判断 | 最高；这是医学人员最有价值的工作 |
| `insufficient_evidence` | 原图缺失、分辨率过低、压缩严重或现有证据不足以可靠判断 | 先获取更高质量官方资产；取得前不要求医学人员凭肉眼强行判断，也不计分 | 不复核；等待资产改善 |

`source_reported` 继续作为“候选来源”字段，而不是直接面向医学人员的任务类型。它不等于算法已经检出，也不等于获得了独立医学标签。医学人员确认公开来源已经报告的内容，最多能作为标签质控；如果同一公开标注同时用于候选生成和结果验收，不能把它称为独立 test。

对于缩放、旋转或翻转后的荧光图，单独并排两张裁剪图通常不足以人工判断。复核包至少应包含：

- 两侧无框原图裁剪及各自在整张 Figure 中的位置；
- 算法给出的变换类型、方向、尺度和置信度；
- 变换后对齐图，以及可切换透明度的叠加图或闪烁对照；
- 差异图、局部特征对应或其他能解释判断的证据；
- 是否已被公开来源报告、为什么需要本次人工复核；
- `重复`、`不重复`、`不确定` 三态结论和简短理由。

建议候选记录补充：

```json
{
  "candidate_origin": "algorithm_novel",
  "source_reported": false,
  "mapping_status": "mapped",
  "machine_status": "candidate_detected",
  "review_priority": "high",
  "review_reason": "算法发现，公开来源未报告",
  "transform_hypothesis": "rotate_180_and_scale",
  "review_status": "not_reviewed",
  "expected": "unknown"
}
```

### 2.5 数据隔离

- `source_group` 至少按论文、同一数据来源或同一母图建立。
- 同一 `source_group` 不得跨越 train、validation 和 test。
- 同一篇论文的原图、裁剪图、标注图、增强图和不同版本必须位于同一 split。
- 跨论文仍应通过文件哈希、感知指纹和候选匹配检查潜在同源泄漏。
- 训练中使用的论文不能再作为独立 test 来源。

## 3. 推荐端到端流程

```text
公众号文章链接或本地文章文件
  -> 获取公开正文和 data-src 图片
  -> 提取公众号文章元数据及重复标注
  -> 解析论文标题、DOI、PMID/PMCID
  -> 核查撤稿/更正记录、许可证和图片单独版权
  -> 从许可明确的来源取得主文 Figure 和 Supplement
     -> 校验实际字节、格式、哈希和附件内容
     -> 验证页/验证码/企业策略阻塞则记录并走合规回退
  -> 匹配 Figure/Panel 并迁移公开来源标记的重复区域
  -> 在干净原图上运行本地候选发现，包括缩放/旋转/翻转等变换
  -> 合并并分流候选
     -> 来源已报告且系统已生成具体 A/B：source_pair_confirm，逐对确认映射与重复结论
     -> 来源已报告但系统未生成具体 A/B：mapping_not_generated，继续由系统处理
     -> 算法发现且来源未报告：novel_candidate，优先医学复核
     -> 资产不足：insufficient_evidence，等待更高质量原图
  -> 医学复核只处理已经给出具体 A/B 证据的来源关系和算法新增候选
  -> 审核通过的明确关系生成正例
  -> 未报告且未形成算法冲突的关系按约定生成默认负例
  -> 按 source_group 隔离数据集
  -> 输出本地训练/评测清单、许可证记录和处理日志
```

首版应是 Excel 驱动的半自动内部流程，不需要先开发 GUI。先处理 5 至 10 篇代表性文章，确认页面解析、论文定位、图片映射和许可证记录格式稳定，再批量处理约 100 篇。重复劳动稳定后再沉淀为命令行批处理工具。

## 4. 建议输入 Excel

最少只要求公众号文章链接，建议字段如下：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `case_id` | 建议 | 本地稳定案例编号 |
| `wechat_url` | 是 | 具体公众号文章链接，不是公众号名称 |
| `known_paper_title` | 否 | 已知论文标题 |
| `doi` | 否 | 已知 DOI |
| `notes` | 否 | 人工提示或重点关系 |
| `priority` | 否 | 处理优先级 |

处理后可回填文章读取状态、论文标识、Figure/Panel、重复关系、干净原图状态、许可证、样本数量、split、异常原因和人工复核状态。

## 5. 建议本地数据记录

每个案例至少保存：

- 公众号 URL、公众号名称、文章标题、发布时间和取得时间；
- 公众号正文和原始标注图片；
- 公众号对重复关系的文字描述；
- 论文标题、作者、期刊、年份、DOI、PMID、PMCID；
- Figure/Panel 编号和两侧区域坐标；
- 裁剪、缩放、旋转、翻转、调色、遮挡等疑似变换；
- 干净原图地址、文章许可证、图片单独版权说明；
- 文件 SHA-256、感知指纹和文章版本；
- `expected`、`label_source`、`review_status`、判断理由；
- `candidate_origin`、`source_reported`、`mapping_status`、`machine_status`、`review_priority` 和 `review_reason`；
- 变换假设、对齐图、叠加图、差异图及局部特征对应等复核证据；
- `source_group` 和 train/validation/test 分组；
- 下载原始 URL、最终 URL、HTTP 状态、预期/实际内容类型、文件格式校验和 SHA-256；
- 获取失败、验证页、验证码、企业策略拦截、映射失败、许可证不清楚等问题。

真实文章、图片、裁剪、清单和结果只保存在 Git 忽略的本地数据目录。除非逐项确认允许再分发，否则不得进入公有仓库或 Release。

## 6. 许可证原则

- 首批自动准入建议限制为 CC0 和 CC BY。
- CC BY-SA、CC BY-NC、CC BY-ND、自定义条款及无明确许可证的内容必须单独评审。
- 即使论文整体开放，也必须检查具体 Figure 是否注明“转载自其他来源”“经许可使用”或存在单独版权；这类图片默认排除。
- Sci-Hub 不作为图片资产来源，不进入工具依赖或数据溯源链路。
- 公众号标注截图与正文的保存、内部使用和再分发条件应与原论文图片许可证分开记录。
- 公开模型权重、公开数据集和内部研究使用是不同的使用场景，进入发布阶段前必须重新完成许可证审查。

当前可参考的官方数据接口：

- PubMed E-utilities：https://www.ncbi.nlm.nih.gov/books/NBK25497/
- Crossref REST API：https://www.crossref.org/documentation/retrieve-metadata/rest-api/
- PMC AWS Article Datasets：https://pmc.ncbi.nlm.nih.gov/tools/pmcaws/
- BioImage Archive：https://www.ebi.ac.uk/bioimage-archive/

## 7. 微信公众号公开文章获取验证

### 7.1 已知可解析结构

首篇试点已经使用公开 HTTPS 请求成功读取微信公众号单篇文章，并验证以下常见页面结构：

- 标题：`#activity-name`
- 公众号名称：`#js_name` 或 `.rich_media_meta_nickname`
- 正文：`#js_content`
- 正文图片：优先读取 `data-src`，部分页面回退到 `src`
- 页面 JavaScript 变量可包含发布时间、`biz` 和账号标识等公开元数据

#### 首选实现方法

新窗口验证公开单篇文章时，首选方法不是 Chrome 浏览器自动化，而是本地终端中的只读 HTTP 获取：

- Python 3.12；
- `requests` 发送普通 `GET` 请求，设置移动端微信或常规浏览器 `User-Agent`、连接/读取超时并允许正常重定向；
- `BeautifulSoup` 使用 `html.parser` 解析返回 HTML；
- 不登录微信，不使用 Cookie、验证码、公众号后台、私有接口或会话令牌；
- 只读取用户明确提供的单篇公开文章 URL，不自动遍历公众号历史文章；
- 若运行环境明确阻止该地址，不得改换通道绕过，改为读取用户导出的 HTML/PDF/Word/图片包。

最小读取示例：

```python
from __future__ import annotations

from dataclasses import dataclass

import requests
from bs4 import BeautifulSoup


HEADERS = {
    "User-Agent": (
        "Mozilla/5.0 (iPhone; CPU iPhone OS 17_0 like Mac OS X) "
        "AppleWebKit/605.1.15 Mobile/15E148 MicroMessenger/8.0.50"
    )
}


@dataclass(frozen=True)
class PublicArticle:
    final_url: str
    status_code: int
    raw_html: bytes
    title: str
    account: str
    body_text: str
    image_urls: tuple[str, ...]


def read_public_article(url: str) -> PublicArticle:
    response = requests.get(
        url,
        headers=HEADERS,
        timeout=(10, 30),
        allow_redirects=True,
    )
    response.raise_for_status()

    soup = BeautifulSoup(response.content, "html.parser")
    content = soup.select_one("#js_content")
    if content is None:
        raise ValueError("页面缺少 #js_content，可能不是完整公开文章")

    title_node = soup.select_one("#activity-name")
    account_node = soup.select_one("#js_name, .rich_media_meta_nickname")
    image_urls = tuple(
        url_value
        for image in content.select("img")
        if (url_value := image.get("data-src") or image.get("src"))
    )

    return PublicArticle(
        final_url=response.url,
        status_code=response.status_code,
        raw_html=response.content,
        title=title_node.get_text(" ", strip=True) if title_node else "",
        account=account_node.get_text(" ", strip=True) if account_node else "",
        body_text=content.get_text("\n", strip=True),
        image_urls=image_urls,
    )
```

该示例只负责取得原始 HTML、完整正文和图片 URL，不包含批量抓取、图片重编码、论文检索或训练样本生成。若把 `requests`、`beautifulsoup4` 引入仓库开发依赖，必须同步依赖约束和许可证登记；一次性验证也可以在独立临时环境运行。

推荐的单篇文章获取步骤：

1. 使用当时安全策略允许的 HTTPS 客户端请求用户给出的完整公开文章 URL，设置合理超时并允许正常重定向；不得使用登录 Cookie、公众号后台凭据或绕过验证页面。
2. 检查最终状态为成功响应、内容确为 HTML，且页面中存在 `#js_content`；如果返回验证页、错误页或空正文，立即标记失败，不把错误页面当成文章。
3. 在修改或清洗前先原样保存响应 HTML，并记录原始 URL、最终 URL、HTTP 状态、取得时间和响应内容哈希。
4. 按上述选择器提取标题、公众号名称和正文；正文同时保存原始 HTML 与可检索纯文本，不能只保存摘要或正文开头。
5. 按 DOM 顺序收集 `#js_content img`，优先使用 `data-src`，缺失时回退 `src`；保留图片原始 URL、所在顺序、附近文字和可能的 Figure/Panel 描述。
6. 下载公开图片时保留原始字节，不做缩放、重编码或去框；使用 URL 和内容哈希去重，但不能因图片重复出现而丢失它在正文中的多个位置关系。
7. JavaScript 变量只用于补充公开发布时间、账号标识等元数据；不能依赖会话令牌、私有接口或公众号管理权限。
8. 输出 `article.json`，至少包含文章 URL、标题、公众号、发布时间、完整正文、图片记录、获取状态、取得时间和问题列表；HTML 与图片放在同一案例的本地目录。
9. 完成后检查正文首尾均存在、DOM 图片数和输出图片记录数可解释、所有失败下载均有原因。任何校验未通过时保留原始材料并进入人工回退，不静默生成不完整样本。

该结果说明单篇公开文章的 HTML 解析路线在技术上可行，但实现必须处理页面变化、访问失败、图片防盗链、重复图片、懒加载地址和个别文章缺失字段。若自动读取失败，应回退到用户导出的完整 HTML、PDF、Word 或长截图，不得绕过站点安全限制。

### 7.2 首篇试点文章

- URL：`https://mp.weixin.qq.com/s/9egbcGwrW3y_2mj6JJK9KQ?scene=1`
- 公众号：Figcheck
- 标题：《细胞全靠PS！这篇Nucleic Acids Research（影响因子=13.1）文章37处图片重复》
- 发布时间：2026-08-12 21:00
- 获取结果：HTTP 200，正文节点完整；原始 HTML 为 3,504,732 字节。
- 正文去空白字符数：2,051。此前“约 2,191 字”属于不同统计口径，不能作为精确验收数。
- 正文中有 47 个 `img` DOM 节点，其中 33 个具有实际 `data-src`/`src`，对应 33 个唯一图片 URL；33 张全部下载成功。
- 第 1–25 张图片与论文定位或重复声明有关，第 26–33 张属于推广或其他非案例内容。
- 原始 HTML、完整正文、33 张原始标注图片、元数据和处理日志均保存在 Git 忽略目录，没有提交到仓库。

这说明单篇公众号文章的公开 HTML 获取和 `data-src` 图片下载路线已得到本窗口独立验证。DOM 图片节点数与实际图片 URL 数必须分别记录，不能把占位节点误报为下载失败。

### 7.3 目标论文、撤稿和许可证核验

从公众号正文唯一定位到：

- 论文：*Real-time monitoring of DNA G-quadruplexes in living cells with a small-molecule fluorescent probe*
- 期刊：Nucleic Acids Research
- DOI：`10.1093/nar/gky665`
- PMID：`30085206`
- PMCID：`PMC6125622`

PubMed、Crossref 和 PMC 元数据相互一致。论文已于 2026-05-26 正式撤稿，撤稿 DOI 为 `10.1093/nar/gkag519`，PMID 为 `42187160`，PMCID 为 `PMC13202614`。官方撤稿说明涉及 Figure 5A、5B、5D、7A、S19、S25，并记录作者承认在 Figure 7 中复制、粘贴细胞以填满空白显微视野。正式撤稿记录可作为公众号标签的独立佐证，但不能自动替代逐区域标注。

论文及主文 Figure 标记为 CC BY-NC 4.0。根据第 6 节规则，本案例不得自动准入可发布数据集，只能先标记为 `license_review_required`，用于内部非商业可行性验证；训练、权重发布、数据再分发等场景仍需单独评审。

已从 PMC 正式文章页面取得 Figure 1–7 的干净 JPEG，并记录来源 URL、尺寸、字节数和 SHA-256。没有把公众号彩框截图作为算法输入。

### 7.4 37 处声明的解析层级

公众号文字中的 37 处可以完整闭合：

| 聚合声明 | 数量 | 干净资产状态 |
| --- | ---: | --- |
| Figure 7A 图内复制 | 4 | Figure 7 已取得 |
| Figure 7A 的 21 张图之间重复 | 21 | Figure 7 已取得 |
| Figure 5 图内复制 | 4 | Figure 5 已取得 |
| Figure S19 图内复制 | 3 | 缺 Supplement |
| Figure 5D 图间重复 | 1 | Figure 5 已取得 |
| Figure 6B 与 Figure S19 | 2 | 缺 Figure S19 |
| Figure 7A 与 Figure 6C | 1 | Figure 6/7 已取得 |
| Figure 7A 与 Figure S25 | 1 | 缺 Figure S25 |
| 合计 | 37 | 31 处只涉及已取得主文 Figure，6 处依赖 Supplement |

必须区分以下三个完成度，不能把它们混称为“映射完成”：

- 聚合文字声明解析：37/37（100%）。
- 当前首轮范围双方均已有干净主文 Figure：31 处。
- 当前 31 处范围内完成精确双区域坐标复核：0/31。

2026-08-28 用户确认首轮试点只使用正式全文中的主文 Figure 1–7。涉及 Supplement S19/S25 的 6 处标为 `deferred_supplement`，暂不进入当前样本范围，也不阻塞其余工作。当前范围因此是 31 处，保守状态为正例 0、默认负例 0、`unknown` 31，另有范围外延后项 6。`source_group` 固定为 `doi:10.1093/nar/gky665`，split 仍为 `unassigned`。

现有 `docs/IMAGE_PAIR_EVALUATION.md` schema 只能运行明确的 `positive`/`negative`，同图比较还要求两个不同区域；它能携带 `label_source`，但目前不能表达聚合声明或 `unknown`，运行结果也不保留可行性流程要求的 `review_status`。精确映射前不得用整张 Figure 或公众号彩框截图强行构造 pair。后续应先提出候选清单 schema，使聚合声明和 `unknown` 能留在预处理层；只有人工审核通过的逐对 `positive`/`negative` 子集才进入现有评测器。

### 7.5 Supplement 获取与安全阻塞处理流程（后续可选）

本案例的 Supplement 是论文正文之外的补充材料 ZIP，PMC 页面标称文件名为 `gky665_supplemental_files.zip`、大小约 4.8 MB，其中需要 Figure S19 和 Figure S25。首轮已决定暂缓 Supplement，以下流程保留给后续扩展案例范围时使用：

1. 只从 PMC、出版社正式页面、JATS 中的补充材料链接或其他许可证明确的官方来源定位附件，记录原始 URL、最终 URL、页面版本和取得时间。
2. 首先使用普通公开 HTTPS 请求；保留原始响应字节，不因文件名以 `.zip` 结尾就假定响应一定是 ZIP。
3. 校验 HTTP 状态、`Content-Type`、文件大小和魔数，并使用 Python 标准库 `zipfile.is_zipfile()` 检查格式。HTML 验证页、错误页或登录页必须记录为失败，不能改名后当作 ZIP。
4. 若官方端点要求浏览器检查，可通过正常、受支持的浏览器打开论文页面并等待自动跳转。遇到验证码时必须交给用户确认或接管，不能自动破解；遇到 HTTPS 安全警告、付费墙或其他安全拦截时不得绕过。
5. 浏览器报告下载完成后，必须再次确认文件确实落盘，记录实际大小和 SHA-256，运行 `ZipFile.testzip()`，列出归档成员，并只解压到 Git 忽略的案例目录。归档中的可执行文件不运行。
6. 若浏览器明确显示“已被您的组织屏蔽”或同类提示，应标记 `blocked_by_enterprise_dlp_policy` 并停止。可以只读检查设备是否受 MDM 管理、浏览器是否存在强制安全扩展和相关下载策略，用于解释原因；不得关闭扩展、修改策略、切换通道或降低系统安全设置来绕过。
7. 企业策略阻塞的合规回退是向单位 IT/安全管理员申请白名单或审批，或通过单位认可的渠道取得同一官方附件。收到用户提供的文件后仍需重新执行来源、格式、哈希和归档内容校验。
8. PMC AWS Article Datasets、OAI 或出版社元数据可用于交叉核验；若当前对象清单只有 XML/JSON/TXT，没有媒体或附件，这只能记录为该分发通道不包含 Supplement，不能推断论文没有补充材料。

本次实际结果是：直接 HTTP 请求得到浏览器验证 HTML，不是有效 ZIP；应用内浏览器和 Chrome 均尝试了官方页面的正常下载流程，Chrome 最终显示该 ZIP“已被您的组织屏蔽”。只读检查确认当前设备受企业管理且存在文件拦截能力，没有尝试关闭安全策略或绕过。Figure S19/S25 因此仍缺失，但已标为后续范围，不再阻塞主文 Figure 试点。

建议为附件获取至少记录：

```json
{
  "supplement_status": "blocked_by_enterprise_dlp_policy",
  "expected_filename": "gky665_supplemental_files.zip",
  "expected_size": "4.8 MB",
  "candidate_is_valid_zip": false,
  "actual_content": "browser_verification_html",
  "clean_figures_missing": ["S19", "S25"],
  "bypass_attempted": false
}
```

不要在受 Git 跟踪的文档或清单中记录单位名称、MDM 服务地址、扩展 ID、内部端口、账号或其他组织安全配置；详细诊断仅保留在本机临时记录中。

### 7.6 医学沟通后的案例适用性结论

本案例已生成 Figure 7 的首版人工复核包：公众号证据图中的彩色框通过图像配准迁移到 PMC 无框 Figure，共形成 10 个颜色组、25 个候选区域，并提供编号总览和逐组放大图。这验证了“公开标记图 -> 正式原图坐标 -> 人工复核图”的技术路线，但不代表 25 个正例，也不能与公众号的“21 处”口径直接互换。

医学协作者进一步确认：该公众号的判断整体上已经较准确；很多荧光图又经过缩放、旋转、翻转或裁剪，单凭肉眼重新判断并不可靠。将这些来源已报告案例全部发给医学人员从零复核，价值较低，而且不能形成独立验证。因此本案例的定位调整为：

- `case_role=workflow_feasibility`：用于验证公众号获取、论文定位、许可证记录、干净 Figure 获取、标注图配准、候选清单和复核图生成；
- `expert_validation_suitability=low`：不要求医学人员完成全部 31 处判断，不作为独立 validation/test；
- 已生成的 Figure 7 复核包属于 `source_reported` 的坐标映射质控示例，当前全部仍为 `unknown/not_reviewed`；
- 若本地算法在正式原图中找到公众号未提到的候选，应另标为 `novel_candidate`，生成对齐证据后再优先请医学人员判断；
- 若公开标记无法可靠迁移，则标为 `mapping_not_generated`；先改善算法证据或原图质量，不要求医学人员仅凭低清并排图强行结论。

新案例在投入论文定位和原图获取前先做适用性筛选：

1. 能否取得分辨率足够、来源和许可证明确的正式原图；
2. 公开标注能否明确落到 Figure/Panel 或局部区域；
3. 是否存在适合算法发现的来源未报告候选，或存在需要独立医学判断的冲突；
4. 经过缩放、旋转、翻转后，能否用对齐、叠加、差异和局部特征证据支持判断；
5. 医学人员的结论是否独立于候选生成来源，并能记录为 `重复/不重复/不确定`；
6. 是否能按论文 `source_group` 隔离，避免进入训练后又作为独立 test。

适用性筛选只决定处理路线、优先级和医学复核方式，不用于删掉用户已经指定的案例。页面难以访问、许可证不清楚、只含 Supplement、跨论文成本高或暂不属于首版图片类型时，也必须保留案例记录和明确状态。

### 7.7 42 个案例的全量输入与公众号获取结果

2026-08-29 用户提供一份本地 Word 文档，内容为 42 组“公众号文章 URL + 简要说明”。文档本身没有嵌入示例图片；42 个 URL 均唯一。说明覆盖 Western blot、荧光、组织染色、流式细胞术、划痕、Transwell、成瘤/活体成像、跨论文复用、仿制图章/拼接、缩放/旋转/反色/对比度变换、DataScan 系统和原始数值关系等场景。

用户明确确认：42 个案例全部需要抓取和保留，不能只选择少量试点。可以为控制执行、失败重试和反馈节奏而分批，但批次不构成筛除；复杂案例同样进入主清单。

第一阶段公众号获取已经完成：

- 42/42 个公开单篇文章 URL 均返回 HTTP 200，并解析到完整 `#js_content` 正文；
- 42/42 个案例均保存原始 HTML、正文 HTML、纯文本和逐篇 `article.json`；
- 共提取 191 个唯一正文图片 URL，191/191 张下载成功；
- 没有验证码页、失效链接、空正文或图片下载缺失；
- 本地批次目录约 167 MB，共 361 个文件，全部位于 Git 忽略目录；
- 公众号标题、账号、文章说明、图片顺序、URL、实际字节数和 SHA-256 均保存在本地清单中。

随后已继续完成论文身份、正式主图、算法候选和交互反馈阶段：

- 42/42 个案例均已建立论文身份记录，共解析出 64 篇去重论文，64/64 均取得 DOI、PMID/PMCID、Crossref 或 Europe PMC 可核对元数据；
- 64 篇论文中 49 篇具有 PMC 全文，47 篇页面实际包含主文 Figure；共下载 306 张正式全文主图，逐图记录官方 URL、caption、尺寸、字节数和 SHA-256，0 张下载失败；
- 其余无 PMC 全文、出版商开放资产未确认或页面本身无 Figure 的论文保留正式状态，不用公众号标记图冒充干净原图；
- 正式主图仅取主文 Figure，Supplement 继续延后；公众号 191 张证据图只用于定位和追溯，没有进入本地算法扫描；
- 本地通用整图/局部几何规则按案例扫描 320 个输入引用，形成 8 条机器候选，分布在 7 个案例，扫描问题为 0；这些候选只是 `novel_candidate`，不视为真实重复；
- 已使用 macOS Vision 在本地对 191 张公众号证据图完成 OCR，未上传图片；再按同色彩框分组，并用 SIFT/单应性把标注区域配准回正式全文 Figure。共检测到 49 个彩框关系组，其中 27 组达到保守阈值并生成具体 A/B 映射，分布于 11 个案例；其余关系继续作为系统待办，不能要求医学人员从头手工定位；
- 交互复核包覆盖 42/42 个案例、64 篇论文、306 张正式正文图和 191 张来源证据图。页面共有 66 张任务/状态卡，但真正需要医学人员确认的只有 35 条：27 条 `source_pair_confirm` 和 8 条 `novel_candidate`；另有 18 条 `mapping_not_generated`、13 条 `insufficient_evidence` 只是逐案例状态卡，均不显示反馈按钮；
- 复核页支持搜索、任务类型/完成状态筛选、正式原图与来源图展开、按任务类型变化的反馈、本地持久化以及反馈 JSON schema 3 导入导出。`source_pair_confirm` 逐对选择“映射正确且重复/映射正确且不重复/不确定/映射错误”，`novel_candidate` 仍按红框或整图 A/B 选择重复/不重复/不确定/关联错误；浏览器已实际验证 42 个案例渲染、图片加载、筛选、反馈保存、刷新恢复以及最终包初始状态为 0/35；
- 首轮医学沟通指出，仅并排展示两张完整 Figure 无法判断程序实际比较的是局部实验图还是整体排版。交互包已据此补充：7 条局部几何候选均生成左右整图红框和红框区域局部放大，1 条整图感知候选明确标注“整图范围”，并展示匹配关键点、几何内点、旋转和尺度等证据；复核人应依据红框区域判断，不再猜测算法命中了哪一块；
- 第二轮医学沟通指出，`mapping_uncertain` 只显示“映射待确认”仍无法说明究竟要确认什么，而且同一案例可能包含多组公开报告关系，不能用一个重复/不重复按钮概括。早期方案曾要求医学人员逐行手工填写 A/B 论文和 Figure，但随后确认这仍把系统应完成的定位工作转嫁给了复核人，因此该方案已废止；
- 第三轮医学沟通明确：公众号标注图本身往往已经写明各重复图片的来源并用同色框标出关系，系统应先读取这些信息、生成逐对关系和正式图定位，再请医学人员确认。第 22 例现已生成两组明确关系（Chinese Medical Journal Figure 4D ↔ UBE2D3 Figure 4A；UBE2D3 Figure 4A ↔ Frontiers in Physiology Figure 4F），并检测到公众号把 UBE2D3 图片标成另一 DOI 的文字/图像冲突，页面会单独提示重点确认；
- 交互包位于 Git 忽略目录 `tmp/wechat-paper-dataset/wechat-42-2026-08-28/interactive-review/`，依赖同批次的本地图片目录，必须整体移动；不得把真实文章、图片或复核结果提交公有仓库。

首轮反馈前，这批结果仍不是训练集或独立 test：27 条来源图片对和 8 条程序候选都尚未完成人工确认，另外 22 个检测到的彩框关系组尚未恢复为具体 A/B。初版交互包的用途是把已形成具体证据的关系交到可追溯复核界面中，同时明确保留未完成的系统待办。

### 7.8 首轮反馈规范化、误报回归与映射返工

2026-08-29 已收到首轮反馈 JSON，`package_id` 与交互包一致，35/35 个必答 `candidate_id` 全部存在且没有多余 ID。原始反馈先以只读副本固定，再产生规范化标签；任何自动解释都不得覆盖原始按钮、理由和时间戳。

原始按钮统计如下：

- 27 条 `source_pair_confirm`：17 条“映射正确且重复”、2 条“映射正确且不重复”、7 条“映射错误”、1 条“不确定”；
- 8 条 `novel_candidate`：5 条“不重复”、3 条“关联错误”。结合书面理由，8 条均明确否定算法候选，没有一条可确认新阳性；
- 有 4 条按钮与理由的语义需要保守归一化。例如第 34 例一条按钮选为“映射正确且不重复”，理由实际说明正式图蓝框与公众号证据对不上，应归为 `mapping_error`，不能误写为负例。

规范化后形成三类结果：

| 结果 | 数量 | 用途 |
| --- | ---: | --- |
| 可用正例 | 17 | 公开来源关系完成首轮医学质控；仍属于 validation 来源，不是独立 test |
| 可用负例 | 9 | 1 条来源关系负例 + 8 条算法困难负例；用于误报回归和规则开发 |
| 暂不计分 | 9 | 8 条需要修正映射，1 条证据不足；返工完成前保持 `unknown` |

这 8 条 `novel_candidate` 固定为 hard-negative validation 回归集。当前通用算法在这 8 条上产生 5 个误报、正确拒绝 3 条，specificity 为 `3/8 = 0.375`；主要误报形态为统计图/折线图/柱状图、组合排版和整图版式相似。该数值只用于定位误报模式，后续围绕这 8 条修改算法后，不能再把同一批样本当成独立测试集或对外准确率。

已确认的来源图片对中，可直接组成 18 条首轮计分关系（17 正、1 负）。当前算法在这 18 条上的回归结果为 9 TP、0 FP、8 FN、1 TN，precision 1.0、recall 0.529、specificity 1.0。该结果同样只是开发期 validation 基线；漏报集中在局部跨图复用、Western blot 小区域和来源映射较细的关系，不能与 8 条困难负例合并宣传为产品准确率。

首轮反馈还暴露出来源映射器的结构性错误：旧实现只在“同一张公众号图中、同一粗色相恰好检测到两个矩形”时生成一对。实际第 1 例的同色关系跨多张来源图，粗色相还会合并不同红/粉色，箭头或框角的小连通域也可能被误当成第二个 Panel。返工规则因此调整为：

1. 允许同一案例内跨来源图按颜色、文字锚点和论文 Figure 关系组配对；
2. 排除面积过小的箭头/框角，禁止“同一 Panel 的局部标记 vs 该 Panel 整体”的自比较；
3. 一条来源关系可拆为多个正式图关系，重复指向同一 A/B 的关系需要去重；
4. Panel 字母不能从整张 Figure 的单个 OCR 锚点机械复制，必须结合正式图位置和医学反馈修正；
5. 单应性投影到正式图边界外、宽高接近零或正式全文不存在对应衍生区域时，标记为不可直接映射，不得强行生成坐标。

本轮已据医学反馈整理出 7 条纠正后的正式全文关系：第 1 例由 3 条错误组合拆分、去重为 5 条跨论文关系；第 25 例两条 A 端 Panel 从错误的 Figure 6L 分别改为 Figure 6K 和 Figure 6I。第 1 例另有一条无效自比较被删除。第 34 例两条公众号后处理/伪彩衍生区域在正式全文主图中没有可一一对应的直接区域，保留来源审计，但排除出“正式全文主图训练集”，不伪造坐标或默认负例。

第 34 例剩余 1 条不确定 Western blot 关系已增加正式图定位、对称裁边、共享对比度、平移对齐、边缘结构和绝对差异证据。裁去两侧 Panel 黑边后，无翻转、仅平移的辅助归一化相关度约为 0.977；该分数只用于生成可比较视图，不能代替医学判断。

据此生成 8 项小范围二次复核包：

- 7 项只确认按首次反馈改正后的正式全文蓝框和 Figure/Panel 是否定位正确，不要求再次判断重复内容；
- 1 项只判断增加证据后的两个条带是 `重复`、`不重复` 还是 `不确定`；
- 页面逐卡写明“究竟需要确认什么”，支持本地保存和导出二次反馈 JSON；
- 已验证 36/36 张页面证据图片正常加载、选择后进度从 0/8 更新为 1/8、刷新后状态保持，浏览器控制台无错误。

2026-08-29 已收到并校验二次反馈 JSON：`package_id` 与小包一致，8/8 个稳定 ID 完整且没有多余项。7 个纠正后的正式图定位全部选择“定位正确”，其中一条书面理由进一步指出存在上下镜像反转；最后 1 个增强条带关系被医学人员判断为“高度重复”。该条带关系在公众号证据图中原本就已由紫色框报告，本轮只是把它映射到正式全文 Figure 5A、补足可读证据并完成医学质控，因此其来源是 `public_source_report`，不是算法新增发现。

两轮反馈合并时初步形成 34 条可计分 validation 关系：25 条 positive、9 条 negative。25 条正例和 1 条来源负例均来自公开来源关系及医学质控；另外 8 条 negative 是算法新增候选被医学否定后形成的困难负例。原始 35 张复核卡与这 34 条关系并非一一对应：第 1 例的错误组合被拆分、去重并删除无效自比较，第 34 例另有 2 条只能在公众号后处理/伪彩图上看到的关系被排除出正式主图训练数据。该数字随后被第 7.9 节的来源框有效性复核修正。

在来源框复核前，算法在这 34 条上的合并基线为 13 TP、5 FP、12 FN、4 TN，precision 0.722、recall 0.520、specificity 0.444。其中 8 条算法困难负例仍为 5 FP/3 TN，说明统计图和整图版式误报仍需优先处理；25 条来源正例中仍有 12 条漏检。该批数据已经参与映射返工、证据设计和误报分析，只能作为 validation/回归集，不能作为独立 test 或对外准确率。

据此开发的 `generic-image-local-2` 对白底统计图/组合版式增加布局抑制，对小尺寸低纹理条带使用有界密集结构复核，对彩色/显微小区域使用受限 SIFT 仿射复核。来源框复核前的 34 条回归为 25 TP、0 FP、0 FN、9 TN：原 5 条版式误报被排除，原 12 条小区域漏检被召回。另用 48 张独立生成的无关灰度小条带形成 1,128 个可能图片对，输出 0 条小区域结果，端到端约 4.5 秒。历史结果保存在 `feedback-v4/final-validation-optimized-result.json`；由于样本已经直接参与错误分析和阈值开发，只能说明已知问题被覆盖，不能称为独立 test、泛化准确率或“100% 准确”。

`panel-split-1` 与 `generic-image-local-2` 合入后，拆分开关保持默认关闭，当时 34 条开发回归仍为 25 TP、0 FP、0 FN、9 TN。这些关系使用的是已经指定好的正式图区域，因此只能证明整合未造成回归，不能评价自动拆图是否正确。另对 `official-papers` 目录中的 299 张完整正式全文图片做了无标签冒烟：11 张整页回退，288 张生成多个候选，共 2,836 个候选、单图最多 46 个，约 8.56 秒；该结果可能同时包含欠拆和过拆，不能据此报告准确率。后续拆图 test 数据应提供完整原图、人工确认的 Panel 边界/层级和来源组，并与重复关系标签分层保存，才能分别计算拆分边界质量和拆分后的重复检出表现。

本地审计产物分别保存原始反馈、规范化标签、hard-negative 清单、两份基线评测、映射返工、纠正关系、训练排除项和二次复核页面。真实正文、图片、反馈和交互包仍只位于 Git 忽略目录。该闭环的最终用途已经明确为：建设可追溯训练集、评测算法并针对 hard negatives 降低误报；不是自动给出学术不端结论。

### 7.9 来源框有效性复核与医学类型路由

2026-08-31 对最终关系中的公众号彩色框补做四边直线连续性检查。18 条直接来源框关系中，17 条存在连续矩形边；病理组织 #02 只有 Azan 染色形成的洋红组织区域，没有完整矩形标记框，属于提取错误。原始反馈保持只读，该关系另记为 `exclude_invalid_source_annotation_detection`，不再作为负例计分。

排除 #02 后，当前有效开发 validation 为 33 条：25 positive、8 negative；医学类型构成为病理 9 条、Western blot 10 条、免疫荧光 2 条，其余通用图片 12 条。`generic-image-local-3`/`dot-blot-4` 按类型运行后的同集结果仍为 25 TP、0 FP、0 FN、8 TN。原先借用 AUTO “局部重复结构”技术检测器召回的 4 条关系，现由 `image.local.geometric` 2 条、`image.small_region.content_reuse` 1 条和 `image.western_blot.single_band` 1 条承接，不再进入 Dot 路径。上述 33 条均参与过开发，只能用于回归，不能作为独立准确率。

### 7.10 AUTO Dot 路由、候选预算与 124 例回归

本次性能诊断确认，AUTO 模式曾在没有 Dot blot 的科研示意图、统计图或拆分 Panel 上直接执行完整斑点提取和组合匹配，导致非 Dot 案例的计算量快速放大。`generic-image-local-3`/`dot-blot-4` 将这条链路改为：

1. AUTO 先用缩放后的廉价形态特征判断页面是否具有斑点阵列征象；常规组件最低短长轴比/圆度为 0.70/0.65，共享基线且厚度近似一致的柱状图柱条被排除，未通过者不进入完整 Dot 提取。
2. 任一 Western 候选区域覆盖页面至少 35%，或同页多个 Western 区域总覆盖至少 35% 且矩形联合覆盖至少 25% 时不再同时进入 AUTO Dot，避免横向条带被错当斑点排列；两个零散小 Western 区域不会屏蔽同页 Dot，用户显式选择 Dot blot 时仍运行完整专项。
3. Dot 子集验证先做廉价布局和轮廓筛选，之后只按实际访问的斑点对计算并缓存旋转/镜像外观相似度，不预先构造完整矩阵，也不再为每个子集重复比较相同斑点对。
4. 通用局部、小区域和 AUTO Dot 候选增加全局与单图/单页对预算；通用候选先覆盖更多不同图片，Dot 候选先覆盖更多不同页面对，再按原排名补足，在固定容量内减少单一密集组垄断。同时使用有界滚动小图候选回退，以常规局部几何和小区域验证承接旧回归关系；固定预算不承诺输入排列不变或完整召回。

兼容性方面，旧 34 条开发回归仍为 25 TP、0 FP、0 FN、9 TN；排除无效来源框后的 33 条口径仍为 25 TP、0 FP、0 FN、8 TN。最终代码的 Panel 三档性能画像中，8、65、471 个 Panel 分别从 135.416799 秒、超过 600 秒、超过 600 秒降至 0.539383 秒、17.965310 秒和 45.745085 秒，均在本地评测的 120 秒 watchdog 内完成；该 watchdog 不是产品内建超时。最终口径额外保留大图内小 Dot 阵列和 Western+Dot 混合页面的 AUTO 召回。

新收集的 124 例（44 阳性、80 阴性）已用于本次路由、超时原因和候选预算诊断，因此已降级为 validation/regression，不能再当作来源独立的 final test。覆盖优先预算、满桶滚动候选与混合页路由修正后，固定 120 秒门限下的整图/Panel 各 124 次重跑均完成，0 超时、0 错误。整图处理 793 个输入、110 条原始结果，总耗时 562.135689 秒，单例 P50/P95/max 为 2.730015/14.558662/27.250835 秒；Panel 处理 7,664 个输入、7,409 条原始结果，总耗时 1,202.536982 秒，单例 P50/P95/max 为 7.058325/27.097468/47.999889 秒。公众号标准答案只能支持案例级筛查：44 个阳性中 35 个有正式资产可评，整图 19 个出现候选、16 个漏过；Panel 35 个均出现候选，9 个因正式资产缺失不可评。80 个阴性在 Panel 模式有 61 个可评，2 个无候选、59 个有候选待医学复核，另 19 个不可评；4,588 条阴性正向候选中通用局部几何占 3,941 条，Dot blot 仅 9 条。该结果不能说明 35 个案例中的具体重复区域已经命中，也不能把 59 个阴性候选直接视为误报；当前没有可计分的 Figure/Panel 区域真值，precision、F1 和区域级召回均为 `N/A`。后续若要评估泛化能力，必须另外收集未参与本次诊断、候选生成和阈值调整的新 `source_group`。

为避免把 7,466 条正向算法发现全部转嫁给医学人员，复核包增加可选的每案例/配置上限：先按 `rule_id` 保留各检测器最高置信度代表，再按置信度补足到 3 条。最终轻量包保留 371 条算法发现，并增加 124 个案例级标签质控任务，共 495 项；被省略的 7,095 条仍保留在原始扫描结果中，省略只控制工作量，不表示“不重复”，也不得计作 FP/FN。ZIP 为 201,012,299 bytes（约 192 MiB），SHA-256 为 `0ea07aa67adb1533e041d88f2467a6bda6cd972a796df2d8fc98b98a82c18fd9`；压缩完整性通过。浏览器验证 124 个案例与 824 张页面图片均正常加载，搜索和反馈刷新保存正常，结构化导出由本地 7 项脚本测试覆盖。来源映射候选没有混入这个算法 validation 包，受限正式图也不嵌入 ZIP。

## 8. 后续接手与医学反馈

1. 按仓库规定依次阅读 `AGENTS.md`、`MEMORY.md`、`docs/STATUS.md`、`docs/PRD.md` 和本文档。
2. 检查 Git 状态；本方向必须在独立开发分支处理，不得混入其他功能分支。
3. 保留首篇试点和 42 案例批次目录，不重新抓取已经完整取得的公众号正文、正式主图和图片；所有真实资料继续只存在于 Git 忽略目录。
4. 首轮 35 项和二次 8 项反馈均已固定并规范化，不再要求医学人员重复填写两个页面；原始反馈、规范化结果和最终标签必须分层保留，不覆盖历史。
5. 以 `feedback-v4/final-reviewed-labels.json` 保留原始标签审计，以 `training-exclusions.json` 记录后续排除项，以 `final-validation-medical-routing-evaluation.json` 和 `final-validation-medical-routing-result.json` 作为当前 33 条有效关系及按医学类型路由的回归结果。
6. 124 例全量复测已完成；冻结当前 33 条、124 例、`generic-image-local-3`/`dot-blot-4` 阈值和当前 `panel-split-1` 基线，不再用这两批样本反复调参。先让医学人员在有界轻量包中复核按案例/模式和规则多样性抽取的代表性候选；反馈只用于确认默认阴性冲突、公开来源映射及主要误报类型。后续算法改动必须同时回归全部 25 条正例、8 条负例、124 例和无关小条带压力测试，并以来源隔离、带人工 Panel 边界的新论文作为拆分与重复判定的泛化依据。
7. 未恢复的 22 个彩框关系组继续改进跨来源图配对、细颜色聚类、OCR 锚点和正式图配准，不交给医学人员从头录入。
8. 无 PMC 正式主图的 13 个案例不得直接生成默认负例；先尝试许可证明确的出版商开放资产，仍不足则继续保持 `insufficient_evidence`。
9. 本批当前 33 条和新 124 例均固定为 validation/regression；另行收集从未参与候选生成、路由诊断、调参和返工的新论文，并按 `source_group` 隔离建立独立 test 后再报告准确率。
10. Supplement 只能按第 7.5 节通过单位认可的渠道获取，不得绕过企业下载策略；本轮交互包不依赖 Supplement。

## 9. 首篇试点完成标准与当前状态

| 验收项 | 当前状态 |
| --- | --- |
| 公众号正文和图片可复核 | 已完成；正文完整，47 个图片节点中 33 个实际图片 URL，33 张下载成功 |
| 唯一论文身份 | 已完成；DOI/PMID/PMCID 相互核对 |
| 撤稿或更正核验 | 已完成；正式撤稿记录可独立佐证部分 Figure |
| 公众号声明数量解析 | 聚合口径 37/37；当前主文范围 31 处，精确 pair/区域 0/31；Supplement 相关 6 处延后 |
| 干净主文 Figure | Figure 1–7 已取得并记录哈希 |
| 干净 Supplement Figure | 当前范围外；S19/S25 延后，不阻塞主文试点 |
| 许可证 | CC BY-NC 4.0，必须单独评审，不能自动准入 |
| 正例/默认负例/unknown | 当前范围为 0/0/31；另有 `deferred_supplement` 6 处 |
| `source_group` 隔离 | 已固定为论文 DOI；split 尚未分配 |
| 彩框污染控制 | 已完成来源分离；公众号截图只作证据，不作模型输入 |
| Figure 7 复核图生成 | 已验证；10 个来源颜色组、25 个候选区域，全部仍为 `unknown/not_reviewed` |
| 独立医学验证适用性 | 低；来源结论较完整且变换荧光图肉眼难判，本案例只保留为流程验证样本 |
| 后续案例输入 | 已接收并完成 42 个唯一公众号案例；42/42 正文完整，191/191 张正文图片下载成功 |
| 后续论文身份与主图 | 64 篇去重论文；49 篇有 PMC 全文；47 篇含主文 Figure，共取得 306 张正式正文图 |
| 后续程序候选 | 通用整图/局部几何扫描形成 8 条 `novel_candidate`，分布于 7 个案例；医学反馈均判为非重复，已固定为困难负例 |
| 后续来源映射 | 191 张来源图完成本地 OCR；检测到 49 个彩框关系组，27 组已恢复成带论文/Figure/区域证据的 `source_pair_confirm`，22 组仍为系统待办 |
| 后续交互复核 | 已生成 42 案例、66 张任务/状态卡的离线 HTML；医学人员只需确认 27 条 `source_pair_confirm` 和 8 条 `novel_candidate`，18 条 `mapping_not_generated` 与 13 条 `insufficient_evidence` 无反馈按钮；反馈 schema 3 |
| 首轮医学反馈 | 已收到并校验 35/35 项；规范化为 17 正、9 负、8 个映射返工、1 个证据返工，原始反馈只读保留 |
| 困难负样本回归 | 8 条算法候选均被否定；当前基线 5 FP/3 TN，主要误报为统计图和整图版式；仅作 validation 回归 |
| 来源关系回归 | 18 条可计分来源关系为 9 TP/0 FP/8 FN/1 TN；仅作开发基线，不是独立准确率 |
| 二次复核 | 已完成 8/8：7 个纠正定位均确认正确，1 个增强条带判断为重复；最后一条是公众号紫框已报告关系，不是算法新增发现 |
| 最终 validation | 来源框复核后为 33 条：25 positive、8 negative；`generic-image-local-3`/`dot-blot-4` 同集开发回归为 25 TP/0 FP/0 FN/8 TN；该批参与调参，不是独立准确率 |
| 现有评测 schema | 只适合审核后的明确 pair，不能完整表达预处理阶段 |
| Git 数据隔离 | 已完成；真实正文、图片、附件响应、清单和报告仅在 Git 忽略目录 |

## 10. 尚未完成

- 当前范围内 31 处主文声明尚未全部转换为无框干净原图上的明确图片对和双区域坐标；由于该案例不适合作为独立医学验证样本，不再要求以人工穷举方式强行完成。
- 已完成两轮反馈和来源框有效性复核，当前固定 33 条 validation：25 条正例和 8 条算法困难负例；原 1 条来源负例因标记框提取无效被排除。新收集的 124 例已用于性能和路由诊断，同样只能作为 validation/regression，尚无来源隔离的独立 test 或可对外准确率。
- 尚未确定候选清单 schema 的正式变更方案；它需要新增候选来源、机器状态、复核优先级和证据包字段，现有运行器仍只接受 `positive`/`negative`。
- 42 个案例已完成论文身份、主文正式图、初轮自动候选和交互反馈包；49 个检测到的公众号彩框关系组中有 27 组已形成具体 A/B，另外 22 组尚未达到可靠配准阈值；13 个案例仍缺少可用主论文正式图。
- 两轮真实反馈已完成“固定原始文件 -> 规范化 -> hard-negative/来源关系基线 -> 映射返工 -> 二次复核 -> 最终 validation 清单 -> `generic-image-local-3`/`dot-blot-4` 同集开发回归”。124 例整图/Panel 各 124 次扫描已在 120 秒门限内完成且 0 超时/0 错误；Panel 对 35 个可评阳性案例均给出候选，但 61 个可评阴性案例中 59 个仍有候选，且没有逐关系区域真值，因此只作为 post-unseal validation。后续先复核有界代表性候选、继续降低通用局部误报，再建立来源隔离新 test；22 个未恢复关系仍为系统待办。
- Supplement 及其 6 处声明已明确延后，不属于当前主文 Figure 试点的完成条件。
- 尚未决定未来使用规则算法调优、监督学习模型或两者结合。
- 尚未更新 PRD，也未授权将该能力纳入第一版产品。
