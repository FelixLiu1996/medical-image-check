# 贡献指南

项目目前仍处于需求阶段。提交任何功能代码前，请先确认对应需求已经进入 `docs/PRD.md`，重大技术选择已经记录为 ADR。

## 基本流程

1. 阅读 `AGENTS.md`、`MEMORY.md` 和 `docs/STATUS.md`。
2. 从最新主分支创建 `codex/<topic>` 或其他清晰命名的功能分支。
3. 保持变更范围单一，补充测试和文档。
4. 使用 Conventional Commits 风格提交。
5. 在变更说明中列出验证命令、结果和已知限制。

## 完成定义

- 功能符合已确认需求。
- CPU 路径正常；涉及 GPU 时验证自动回退。
- 不修改用户原始数据。
- 自动化测试通过。
- 文档、状态、交接信息和用户可见变更已同步。
- 新窗口只依赖仓库内容即可理解和继续工作。

## 测试数据

禁止提交真实或授权不明确的实验数据。测试数据必须是人工生成、明确公开授权或经确认允许在公有仓库分发的数据。
